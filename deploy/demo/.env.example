# Non-production Demo only. Do not reuse production credentials or domains.
DEMO_DB_PASSWORD=replace-with-a-demo-only-database-password
DEMO_JWT_SECRET=replace-with-a-random-demo-only-secret-at-least-32-characters
DEMO_USER_USERNAME=demo-user
DEMO_USER_EMAIL=demo@example.invalid
DEMO_USER_PASSWORD=replace-with-a-demo-only-login-password
DEMO_WEB_PORT=18080

# Required only by scripts/ops/reset-demo-environment.sh.
DEMO_RESET_CONFIRM=RESET_DEMO_ENVIRONMENT
