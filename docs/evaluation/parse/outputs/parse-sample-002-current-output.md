# parse-sample-002 当前输出记录

## 运行方式

本记录为 v2.9.7 的人工评估基线，基于 v2.9.2-v2.9.6 已落地规则对样例文本进行预期输出记录；未调用真实上传接口，不包含真实附件。

## 当前输出摘要

| 字段 | 当前结果 |
|---|---|
| textQualityStatus | GOOD |
| parseQualityStatus | WARNING |
| 主要警告 | 可能存在 `SECTION_TOO_FEW`、`PROJECTS_MISSING` 或 `SKILLS_MISSING` |

## 命中字段

- 可命中 Python、JavaScript、Vue。
- 可识别 `项目经历` 章节后的项目内容。
- 可保留原始文本、清洗文本和章节识别结果供人工检查。

## 漏识别字段

- `技术能力` 当前不一定稳定归一到 `SKILLS`，可能导致技能区质量提示。
- FastAPI、LangChain、Prompt Engineering、RAG、向量检索是否进入技能列表仍依赖关键词覆盖。
- `智能知识库问答系统` 可能被识别为章节内普通行，但项目名称/描述边界仍不稳定。

## 错误分类字段

- 表格展平后的技能分类行可能被当作普通文本。

## 文本提取问题

- DOCX 表格语义丢失，分类层级被展平。

## 结论

当前规则能把异常展示出来，但技能别名、AI 技术关键词覆盖和项目名称/描述分段仍是后续优化重点。
