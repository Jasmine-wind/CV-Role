# parse-sample-001 当前输出记录

## 运行方式

本记录为 v2.9.7 的人工评估基线，基于 v2.9.2-v2.9.6 已落地规则对样例文本进行预期输出记录；未调用真实上传接口，不包含真实附件。

## 当前输出摘要

| 字段 | 当前结果 |
|---|---|
| textQualityStatus | GOOD |
| parseQualityStatus | WARNING |
| 主要警告 | 可能存在 `TEXT_STRUCTURE_MISMATCH` 或字段边界不完整 |

## 命中字段

- 技能关键词可命中 Java、Spring Boot、MyBatis-Plus、Spring Security、Redis、JWT。
- 教育经历可命中 `示例理工大学 软件工程 2023.09 - 2027.06`。
- 项目经历可识别 `校园二手交易平台` 和 `简易博客系统` 的部分内容。

## 漏识别字段

- `获奖经历 校级程序设计竞赛三等奖` 可能因标题和内容在同一行而被部分漏识别。
- 项目职责与成果仍可能保留为扁平行，缺少项目内层级。

## 错误分类字段

- 若项目名称出现在技能章节附近，仍需关注是否误入技能列表。

## 文本提取问题

- PDF 文本顺序混乱。
- 项目符号丢失。

## 结论

相比 v2.9.1，当前规则能提供质量提示并保留章节结果，但对强顺序混乱 PDF 的项目内部结构仍只能做基础恢复。
