# v3.2 面向领域的包结构整理

## 目标

在不改变核心业务行为的前提下，整理后端代码包结构，使用户、简历、岗位、AI 分析、历史、向量、存储和安全等职责边界更清晰。

## 完成内容

| 小任务 | 状态 | 说明 |
|---|---|---|
| v3.2.1 目标包结构确认 | 已完成 | 已扫描当前后端包结构，确认目标包结构、暂不移动范围和后续可小步整理范围 |
| v3.2.2 领域模块边界整理 | 已完成 | 已将 4 个 AI 输出解析实现类移动到 diagnosis / match / suggestion / rewrite 子领域包，并保留接口和业务入口不变 |
| v3.2.3 Controller / Service 职责整理 | 已完成 | 已抽出 `AnalysisVoAssembler`，将分析相关 VO 组装和 JSON 展示解析从 Controller 下沉到 assembler |
| v3.2.4 基础设施层职责整理 | 已完成 | 已将 Embedding Client 接口、配置和 OpenAI-compatible 实现从 `infra.ai` 移动到 `infra.embedding` |
| v3.2.5 v3.2 联调、审查与日志 | 已完成 | 已完成后端编译、相关单元测试、架构边界审查和文档收口，v3.3 已切为当前版本 |

## 验证结果

- 已扫描 `backend/src/main/java/com/winter/airesumeoptimizer` 当前包结构。
- 已确认当前一级包为 `common / config / security / infra / module`。
- 已确认业务模块集中在 `module/auth`、`module/user`、`module/resume`、`module/job`、`module/analysis`、`module/history`、`module/embedding`。
- 已更新 `docs/tasks/phase-4-task-list.md`，将 v3.2.1 标记为已完成。
- 已移动 `ResumeAnalysisOutputParserImpl` 到 `module/analysis/diagnosis/service/impl`。
- 已移动 `AiJobMatchOutputParserImpl` 到 `module/analysis/match/service/impl`。
- 已移动 `AiResumeSuggestionOutputParserImpl` 到 `module/analysis/suggestion/service/impl`。
- 已移动 `AiRewriteSuggestionOutputParserImpl` 到 `module/analysis/rewrite/service/impl`。
- 已同步移动上述 4 个实现类的单元测试包路径。
- 已更新 `docs/tasks/phase-4-task-list.md`，将 v3.2.2 标记为已完成，并把 v3.2.3 标记为当前任务。
- 本轮只做低风险包结构移动，不修改接口路径，不修改数据库结构，不改变业务行为。
- 已新增 `module/analysis/assembler/AnalysisVoAssembler`，统一组装简历诊断、匹配分析、岗位优化建议和局部改写相关 VO。
- 已从 `ResumeAnalysisController` 移除 Entity / JSON 到 VO 的私有转换方法，Controller 只保留请求入口、当前用户读取、Service 调用和 Result 返回。
- 已复用 `AnalysisVoAssembler` 处理 `AiRewriteSuggestionController` 的采纳状态返回 VO，消除重复改写 VO 组装逻辑。
- 已扫描 Controller、报告 Service 和历史 Service，未发现 Controller 拼 Prompt 或报告 / 历史模块调用 AI Client 触发新生成。
- 已更新 `docs/tasks/phase-4-task-list.md`，将 v3.2.3 标记为已完成，并把 v3.2.4 标记为当前任务。
- 已扫描 AI Client、Prompt 加载、Embedding Client、文件存储和向量查询相关代码。
- 已保留 AI Completion 相关类在 `infra.ai`，保留文件存储相关类在 `infra.storage`。
- 已将 `EmbeddingClientService`、`EmbeddingClientProperties`、`OpenAiCompatibleEmbeddingClientService` 移动到 `infra.embedding`。
- 已同步移动 Embedding Client 相关测试包路径。
- 已更新业务层 import，`ResumeEmbeddingServiceImpl` 和 `JobDescriptionEmbeddingServiceImpl` 继续只依赖 `EmbeddingClientService` 接口。
- 已更新 `docs/tasks/phase-4-task-list.md`，将 v3.2.4 标记为已完成，并把 v3.2.5 标记为当前任务。
- 已更新 `docs/project-structure.md`，补充 `infra.embedding` 的职责边界。
- 已完成 v3.2 架构边界审查：接口路径未改、数据库结构未改、前端未改，报告和历史模块未触发新 AI 生成。
- 已更新 `docs/tasks/phase-4-task-list.md`，将 v3.2 标记为已完成，并把 v3.3 标记为当前版本。

验证结果：

- 迁移相关 4 个测试类共 17 个测试通过。
- Embedding 基础设施和业务编排相关 4 个测试类共 10 个测试通过。
- 后端主代码和测试代码编译通过，测试执行按 `-DskipTests` 跳过。
- 本轮未修改前端页面、路由或 API 调用，未执行前端构建；前端主流程保留给用户手动验收。
- `./mvnw test` 全量执行时，编译已通过

## 当前不足

- v3.2 只做低风险包结构整理和 assembler 提取，尚未继续拆分 AI Prompt 模板加载、pgvector SQL 或大 Service。
- `ResumeServiceImpl`、简历解析 / 展示相关实现类等大类仍保留原位。
- `infra.ai` 仍同时承载 AI Completion 和 Prompt 模板加载，后续可继续小步细分。
- 前端主流程未在本轮自动验收，需要用户按任务清单手动确认。

## 结果

v3.2 已完成面向领域的包结构整理，可以进入 v3.3 文件存储抽象与存储策略演进。后续仍应遵守小步低风险原则，不做批量包迁移，不改接口路径，不改数据库结构。
