# v1.8 - 本地编排、CI 与文档收口

## 目标

完成 Phase 2 工程化增强阶段收口，使项目具备更容易复现的本地依赖环境、基础 CI 检查和清晰的阶段归档文档。

## 完成内容

- 新增根目录 `docker-compose.yml`。
- 编排 PostgreSQL 本地服务，默认数据库为 `ai_resume_optimizer`。
- 编排 MinIO 本地服务，预留对象存储依赖环境。
- 更新 `.env.example`，补充后端端口、PostgreSQL、MinIO、AI、JWT 和本地存储配置占位符。
- 新增 `.github/workflows/ci.yml`。
- CI 后端任务使用 Java 21，并通过 PostgreSQL 服务容器执行 `./mvnw test`。
- CI 后端任务显式使用 `test` profile，避免上下文测试误走本地 `dev` 配置。
- CI 前端任务使用 Node 22，执行 `npm ci` 和 `npm run build`。
- 更新 README，补充 Compose 启停、本地依赖端口、接口文档、CI 和任务文档入口。
- 新增 `docs/codex-context.md`，将项目上下文切换为 Phase 2 已完成，下一阶段进入 Phase 3。
- 更新 `docs/tasks/phase-2-task-list.md`，标记 v1.8 和 Phase 2 已完成。
- 更新 `docs/phase-2-engineering.md`，明确下一步进入 Phase 3。

## 本地依赖服务

启动 PostgreSQL 和 MinIO：

```bash
docker compose up -d postgres minio
```

或使用 Podman：

```bash
podman compose up -d postgres minio
```

默认端口：

```text
PostgreSQL: localhost:5432
MinIO API:  http://localhost:9000
MinIO 控制台: http://localhost:9001
```

当前后端上传链路仍使用本地文件存储，MinIO 只作为后续切换对象存储时的本地依赖预留。

## CI

CI 文件：

```text
.github/workflows/ci.yml
```

覆盖内容：

- 后端测试：`./mvnw test`
- 前端依赖安装：`npm ci`
- 前端构建：`npm run build`


## 验证结果

已执行后端测试：

```bash
cd backend
./mvnw test
```


已执行前端构建：

```bash
cd web
npm run build
```

已检查 Compose 配置：

```bash
docker compose config
```

已执行敏感信息关键字检查，当前新增配置只包含占位符和本地默认开发密码，不包含真实 API Key 或私钥。

## Phase 2 收口状态

Phase 2 已完成以下工程化目标：

- 配置分离。
- 统一异常、错误码和参数校验。
- Flyway 数据库迁移。
- 日志规范与敏感信息保护。
- Service 层基础单元测试。
- 关键 API 接口级测试。
- OpenAPI / Swagger UI 接口文档。
- PostgreSQL / MinIO 本地依赖编排。
- GitHub Actions 基础 CI。
- README 和阶段文档收口。

## 后续

下一阶段进入 Phase 3 AI 能力深化。后续改动应继续保持 Phase 1 主流程可运行，并以 Phase 2 的测试、日志、配置和文档基线作为保护边界。
