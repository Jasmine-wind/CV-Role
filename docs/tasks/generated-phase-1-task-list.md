# Phase 1 Task List

## 0. 文件用途

本文件用于拆解 Phase 1 的具体开发任务。

`docs/phase-1-mvp.md` 负责说明 Phase 1 要做什么。  
本文件负责说明每个版本下面具体怎么拆小任务。

使用原则：

1. 每次只做一个小任务。
2. 每个小任务都要有目标、任务、验收标准。
3. 完成一个 v0.x 版本后，更新对应迭代日志。
4. 不允许跳过当前任务直接实现后续版本功能。

---

## 1. Phase 1 总版本划分

| 版本 | 主题 | 状态 |
|---|---|---|
| v0.1 | 项目基线与用户认证 | 已完成 |
| v0.2 | 简历上传与文件存储 | 已完成 |
| v0.3 | 文本提取与结构化解析 | 已完成 |
| v0.4 | AI 简历分析 | 已完成 |
| v0.5 | 岗位匹配与优化建议 | 已完成 |
| v0.6 | 历史记录、联调与演示 | 已完成 |

---

# v0.1 - 项目基线与用户认证

状态：已完成

## v0.1 总结

v0.1 已完成前后端认证基础闭环：

- 后端项目基线确认完成。
- `users` 表、User 实体、UserMapper 已完成。
- 注册接口 `POST /api/auth/register` 已完成。
- 登录接口 `POST /api/auth/login` 已完成。
- JWT 生成、校验和过滤器已完成。
- Spring Security 无状态鉴权已完成。
- 当前用户接口 `GET /api/users/me` 已完成。
- 前端 Vue 3 + TypeScript + Vite 基线已确认。
- 登录页、注册页和基础首页已完成。
- Pinia 认证状态管理已完成。
- Axios 请求封装和路由守卫已完成。
- 后端 CORS 已完成本地开发配置。
- 前后端注册、登录、获取当前用户联调已通过。
- 迭代日志已更新：`docs/iteration-log/v0.1-auth.md`。

## v0.1 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.1.1 技术基线确认 | 已完成 |
| v0.1.2 数据库与用户表 | 已完成 |
| v0.1.3 用户模型与基础分层 | 已完成 |
| v0.1.4 用户注册接口 | 已完成 |
| v0.1.5 用户登录与 JWT 生成 | 已完成 |
| v0.1.6 Spring Security 与 JWT 鉴权 | 已完成 |
| v0.1.7 当前用户接口 | 已完成 |
| v0.1.8 前端项目基线确认 | 已完成 |
| v0.1.9 前端登录注册页面 | 已完成 |
| v0.1.10 前端认证状态管理 | 已完成 |
| v0.1.11 Axios 请求封装与路由守卫 | 已完成 |
| v0.1.12 前后端认证联调、审查与日志 | 已完成 |


---


# v0.2 - 简历上传与文件存储

状态：已完成

## v0.2 总结

v0.2 已完成简历上传与文件存储闭环：

- 已设计 `resumes` 表，并创建 Resume 实体和 ResumeMapper。
- 已实现本地兼容文件存储服务，文件保存到 `uploads/`。
- 已实现 `POST /api/resumes` 简历上传接口。
- 已支持 PDF、DOC、DOCX，文件大小限制为 10 MB。
- 已保存上传用户、原始文件名、文件类型、文件大小、对象 key、上传状态和上传时间。
- 已实现 `GET /api/resumes` 简历列表接口。
- 已实现 `GET /api/resumes/{id}` 简历详情接口。
- 已保证用户只能查看自己的简历记录。
- 已实现前端 `/resumes` 页面，支持上传简历和展示当前用户简历列表。
- 已完成前后端联调，验证 PDF/DOC/DOCX 上传、`.txt` 拒绝、大写后缀、换用户上传和数据库记录。
- 已修复本地 CORS、Word 文件 Content-Type 兼容、multipart 大小配置和上传异常提示问题。
- 已运行后端 `./mvnw -DskipTests package`。
- 已运行前端 `npm run build`。
- 迭代日志已更新：`docs/iteration-log/v0.2-upload.md`。

## v0.2 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.2.1 简历表设计 | 已完成 |
| v0.2.2 文件存储服务 | 已完成 |
| v0.2.3 文件上传接口 | 已完成 |
| v0.2.4 简历列表和详情接口 | 已完成 |
| v0.2.5 前端简历上传与列表 | 已完成 |
| v0.2.6 联调、审查与日志 | 已完成 |

---

# v0.3 - 文本提取与结构化解析

状态：已完成

## v0.3 总结

v0.3 已完成简历文本提取与基础结构化解析闭环：

- 已设计 `resume_parse_results` 表，并创建 ResumeParseResult 实体和 ResumeParseResultMapper。
- 已通过 `resume_id` 关联 `resumes.id`，并限制一份简历只保留一条最新解析结果。
- 已集成 Apache PDFBox 和 Apache POI，支持 PDF、DOC、DOCX 简历文本提取。
- 已在文件存储服务中增加 `open(String objectKey)`，支持通过已上传简历的对象 key 重新读取文件。
- 已实现 `ResumeTextExtractionService`，处理不支持类型、空文本和文件读取失败等异常。
- 已实现 `ResumeStructureParseService`，基于规则提取姓名、手机号、邮箱、技能关键词、教育经历、项目经历和实习或工作经历。
- 已实现 `POST /api/resumes/{id}/parse` 解析触发接口。
- 已实现 `GET /api/resumes/{id}/parse-result` 解析结果查询接口。
- 已保证用户只能解析和查看自己的简历解析结果。
- 已实现重复解析覆盖旧结果，解析失败时保存 `FAILED` 状态和错误信息。
- 已实现前端 `/resumes` 页面中的“开始解析”和“查看结果”操作。
- 已展示解析状态、原始文本、基础结构化字段、技能关键词和经历列表。
- 已补齐简历删除闭环：`DELETE /api/resumes/{id}`、前端删除按钮、删除后刷新列表和清空当前解析面板。
- 已完成前后端联调，验证 PDF/DOCX 文本提取、解析结果保存和查询、重复解析覆盖、用户数据隔离和解析失败提示。
- 已运行后端 `./mvnw -DskipTests package`。
- 已运行前端 `npm run build`。
- 迭代日志已更新：`docs/iteration-log/v0.3-parse.md`。

说明：

- v0.3 不调用 AI，不做岗位匹配，不生成简历优化建议。
- 当前基础结构化解析以规则为主，字段划分会受简历模板影响。
- 后续 v0.4 AI 简历分析应优先使用准确的 `extracted_text`，`structured_json` 只作为辅助信息。
- 图片型简历、扫描型简历、复杂 OCR 不属于 v0.3 支持重点。

## v0.3 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.3.1 解析结果表设计 | 已完成 |
| v0.3.2 文本提取服务 | 已完成 |
| v0.3.3 基础结构化解析服务 | 已完成 |
| v0.3.4 解析触发接口 | 已完成 |
| v0.3.5 解析结果查询接口 | 已完成 |
| v0.3.6 前端解析结果展示 | 已完成 |
| v0.3.7 联调、审查与日志 | 已完成 |

---

# v0.4 - AI 简历分析

状态：已完成

## v0.4 总结

v0.4 已完成 AI 简历分析基础闭环：

- 已设计 `resume_ai_analyses` 表，并创建 `ResumeAiAnalysis` 实体和 `ResumeAiAnalysisMapper`。
- 已完成 OpenAI 兼容 AI 调用适配，支持 API Key、base-url、模型名称、超时时间、温度和输出 token 限制配置。
- 已完成第一版 AI 简历分析 Prompt，约束模型只基于解析内容输出 JSON。
- 已完成 AI 输出解析器，将模型输出转换为系统内部 DTO。
- 已实现 `POST /api/resumes/{id}/ai-analysis` 触发 AI 分析。
- 已实现 `GET /api/resumes/{id}/ai-analysis` 查询 AI 分析结果。
- 已保证用户只能分析和查看自己的简历。
- 已实现重复分析覆盖旧结果，失败分析保存 `FAILED` 状态和错误信息。
- 已完成前端 `/resumes` 页面中的 AI 分析触发、查询和结果展示。
- 已优化 AI 分析长文本输入、PDF 换行噪声、前端请求超时和分析结果展示状态。
- 已补齐删除简历时清理 AI 分析、解析结果、简历记录和本地文件的闭环。
- 已完成 v0.4 前后端联调，AI 分析闭环可演示。
- 已运行后端 `./mvnw test`。
- 已运行前端 `npm run build`。
- 迭代日志已更新：`docs/iteration-log/v0.4-ai-analysis.md`。

说明：

- v0.4 不做岗位匹配，不生成针对具体岗位的优化建议。
- 当前 Prompt 是 Phase 1 基础版本，后续可在真实使用中继续微调。
- 当前长文本处理只做轻量压缩和清洗，不引入 RAG、分片总结或向量检索。

## v0.4 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.4.1 AI 分析结果表设计 | 已完成 |
| v0.4.2 Spring AI 基础接入 | 已完成 |
| v0.4.3 Prompt 模板与 AI 输出结构 | 已完成 |
| v0.4.4 AI 简历分析服务 | 已完成 |
| v0.4.5 AI 分析触发接口 | 已完成 |
| v0.4.6 AI 分析结果查询接口 | 已完成 |
| v0.4.7 前端 AI 分析结果展示 | 已完成 |
| v0.4.8 联调、审查与日志 | 已完成 |

---

# v0.5 - 岗位匹配与优化建议

状态：已完成

## v0.5 总结

v0.5 已完成岗位匹配与优化建议基础闭环：

- 已设计 `jobs` 表，并创建 Job 实体和 JobMapper。
- 已通过 SQL 初始化脚本预置 Phase 1 可用岗位数据。
- 已实现 `GET /api/jobs` 岗位列表接口。
- 已实现 `GET /api/jobs/{id}` 岗位详情接口。
- 已设计 `job_match_results` 表，并创建 JobMatchResult 实体和 JobMatchResultMapper。
- 已实现基础关键词匹配算法，生成匹配分数、命中项、缺失项和匹配说明。
- 已实现基于匹配差距的规则优化建议生成。
- 已实现 `POST /api/resumes/{resumeId}/job-matches` 岗位匹配触发接口。
- 已实现 `GET /api/resumes/{resumeId}/job-matches` 岗位匹配结果查询接口。
- 已保证用户只能匹配和查看自己的简历匹配结果。
- 已实现重复匹配覆盖同一简历与同一岗位的旧结果。
- 已实现前端岗位列表页和岗位详情页。
- 已在岗位详情页支持选择简历、触发匹配、展示匹配结果和优化建议。
- 已处理无可用简历、未解析简历、岗位不可用、匹配失败和加载状态。
- 已补齐删除简历时清理岗位匹配结果的闭环。
- 已完成 v0.5 前后端联调，岗位匹配与优化建议闭环可演示。
- 已运行后端 `./mvnw test`。
- 已运行前端 `npm run build`。
- 迭代日志已更新：`docs/iteration-log/v0.5-job-match.md`。

说明：

- v0.5 不做完整岗位管理后台。
- v0.5 不做向量语义匹配、RAG 或 pgvector。
- v0.5 不做复杂推荐系统或定制版简历生成。
- Phase 1 匹配范围以“单份简历 + 单个岗位”为主。

## v0.5 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.5.1 岗位表设计与预置数据 | 已完成 |
| v0.5.2 岗位列表与详情接口 | 已完成 |
| v0.5.3 岗位匹配结果表设计 | 已完成 |
| v0.5.4 基础关键词匹配算法 | 已完成 |
| v0.5.5 基础优化建议生成 | 已完成 |
| v0.5.6 岗位匹配接口 | 已完成 |
| v0.5.7 岗位匹配结果查询接口 | 已完成 |
| v0.5.8 前端岗位列表与岗位详情 | 已完成 |
| v0.5.9 前端匹配结果与优化建议展示 | 已完成 |
| v0.5.10 联调、审查与日志 | 已完成 |

---

# v0.6 - 历史记录、联调与演示

状态：已完成

## v0.6 总结

v0.6 已完成历史记录、联调与演示收口：

- 已完成历史记录聚合查询方案，不新增复杂历史流水表。
- 已实现 `GET /api/history` 当前用户历史记录分页列表接口。
- 已实现 `GET /api/history/{resumeId}` 当前用户单份简历历史详情接口。
- 已聚合简历上传、解析状态、AI 分析状态和最近岗位匹配结果。
- 已保证历史记录接口只能访问当前用户自己的数据。
- 已实现前端 `/history` 历史记录页面。
- 已在首页、简历页和岗位详情页增加历史记录入口。
- 已支持从历史记录跳转到对应简历和岗位匹配上下文。
- 已完成 Phase 1 主流程联调，用户确认全流程测试全部通过。
- 已统一前端常见错误提示，包括 Token 失效、超时、后端不可连接和服务器错误。
- 已补充后端全局异常处理日志，便于排查演示过程中的错误。
- 已更新 README，说明项目结构、环境变量、本地启动、数据库初始化和 Phase 1 演示流程。
- 已确认当前 Phase 1 使用本地 `uploads/` 文件存储，不需要启动 MinIO。
- 已完成 v0.6 迭代日志与 Phase 1 当前状态更新。
- 已完成最终检查，确认后端测试、前端构建、数据库脚本、`.gitignore`、敏感信息和 Git 状态。

说明：

- v0.6 不做复杂历史流水、管理员后台、复杂统计、RAG、pgvector、向量语义匹配或微服务拆分。
- 历史记录采用 Service 层聚合查询，适合 Phase 1 演示数据量。
- Phase 1 主流程已形成“注册登录 -> 上传简历 -> 解析简历 -> AI 分析 -> 岗位匹配 -> 优化建议 -> 历史记录”的完整闭环。
- 后续 Phase 2 可继续处理 Flyway、MapStruct、统一错误码、接口文档、管理后台和工程化部署。

## v0.6 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v0.6.1 历史记录聚合查询设计 | 已完成 |
| v0.6.2 历史记录接口实现 | 已完成 |
| v0.6.3 历史记录前端页面 | 已完成 |
| v0.6.4 Phase 1 全流程联调 | 已完成 |
| v0.6.5 错误处理与体验修复 | 已完成 |
| v0.6.6 README 与本地启动说明 | 已完成 |
| v0.6.7 迭代日志与项目状态更新 | 已完成 |
| v0.6.8 最终检查与提交 | 已完成 |
