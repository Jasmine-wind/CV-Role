# Phase 3 Task List - AI 能力增强阶段

## 0. 文件用途

本文件用于拆解 Phase 3 的具体开发任务。

`docs/phase-3-ai.md` 负责说明第三阶段的总体目标、功能范围、技术范围和阶段边界。  
本文件负责说明 Phase 3 每个版本下面具体怎么拆小任务。

使用原则：

1. 每次只做一个小任务。
2. 当前正在开发的 v2.x 版本详细展开。
3. 已完成版本压缩成总结。
4. 未开始版本可以先保留大纲，进入对应版本前再继续细分。
5. 完成一个 v2.x 版本后，更新对应迭代日志。
6. 不允许跳过当前任务直接实现后续版本功能。
7. AI 输出必须遵守事实一致性原则，不得编造用户不存在的经历、技能、证书、奖项或量化指标。
8. 所有 AI 输出应尽量结构化，关键结果必须保存模型名称、Prompt 版本和生成时间。

---

## 1. Phase 3 总版本划分

| 版本 | 主题 | 状态 |
|---|---|---|
| v2.1 | AI 能力基线检查与提示词规范 | 已完成 |
| v2.2 | 岗位描述输入与结构化解析 | 已完成 |
| v2.3 | 简历与岗位 AI 匹配分析 | 已完成 |
| v2.4 | AI 简历优化建议增强 | 已完成 |
| v2.5 | AI 局部改写建议 | 已完成 |
| v2.5.5 | 产品信息架构与模块边界收敛 | 已完成 |
| v2.6 | AI 结果持久化与历史回看 | 已完成 |
| v2.7 | AI 输出评估流程 | 当前版本 |
| v2.8 | Embedding / pgvector / RAG 初步增强 | 未开始 |

---

## 2. Phase 3 总目标

在 Phase 1 MVP 和 Phase 2 工程化基础上，增强系统的 AI 能力，使系统能够围绕“简历内容 + 岗位描述”完成更深入的分析、匹配、优化和局部改写。

Phase 3 完成后应满足：

- 用户可以提交岗位描述。
- 系统可以解析岗位描述并抽取结构化岗位要求。
- 系统可以基于简历和岗位描述生成结构化匹配反馈。
- 系统可以生成具体、有依据的简历优化建议。
- 系统可以给出局部改写建议。
- AI 输出不会编造用户不存在的经历。
- AI 输出可以持久化和回看。
- AI 输出记录模型名称和 Prompt 版本。
- 系统具备基础 AI 输出评估流程。
- 可选完成基础 Embedding / pgvector / RAG 增强。

---

## 3. Phase 3 禁止提前实现内容

在 Phase 3 中不允许提前实现：

- 全自动职位投递。
- 模型微调。
- 企业级 Prompt 实验平台。
- 大规模招聘网站爬取。
- 自动伪造简历经历。
- 自动生成不存在的证书、奖项、量化指标。
- 复杂 A/B 测试平台。
- 多租户企业后台。
- 微服务拆分。
- RabbitMQ / Nacos / Gateway。
- Kubernetes。
- 复杂监控平台。
- 生产级高可用部署。

---

# v2.1 - AI 能力基线检查与提示词规范

状态：已完成

## v2.1 总结

v2.1 已完成 AI 能力基线检查与提示词规范收口：

- 已确认当前 AI 调用入口位于 `infra/ai/`，核心为 `AiClientService`、`AiClientProperties` 和 `OpenAiCompatibleAiClientService`。
- 已确认 AI 配置通过 `application.yaml` 读取环境变量，模型名称由 `OPENAI_MODEL` 注入，不在业务代码中硬编码。
- 已确认当前唯一业务 AI 能力是“简历 AI 分析”，由 `ResumeAnalysisServiceImpl` 编排。
- 已确认 Prompt 构建和输出解析已拆分到独立 Service，Prompt 版本为 `resume_analysis_v1`。
- 已明确 `infra/ai/`、Prompt 构建、输出解析、业务 Service、Entity / Mapper 和 VO 的分层职责。
- 已更新 `docs/project-structure.md`，补充 AI 分层和后续扩展放置规则。
- 已整理 Prompt 命名、版本、输入字段、输出字段、JSON 约束和安全约束规范。
- 已整理 AI 输出解析流程，明确非 JSON、字段缺失、评分越界、多余字段和脏数据入库策略。
- 已在现有简历分析 Prompt 中补充不得代填量化指标、证书、奖项或项目结果的约束。
- 已在 AI 输出解析中限制单条列表文本长度，避免超长自由文本直接进入结构化结果。
- 已在简历 AI 分析展示和历史 AI 分析摘要中增加“AI 建议需用户确认”的提示。
- 已在 README 中补充 AI 输出限制说明。
- 已确认当前日志不完整打印简历原文，只记录必要状态、模型和脱敏错误原因。
- 已完成 v2.1 相关单元测试、前端构建和文档静态核对。
- 迭代日志已更新：`docs/iteration-log/v2.1-ai-baseline.md`。

说明：

- v2.1 不新增岗位描述解析、AI 匹配、优化建议增强或局部改写。
- v2.1 不引入复杂 Prompt 管理后台、Prompt A/B 测试平台、RAG、pgvector 或微服务拆分。
- 当前 Prompt 和解析服务仍保留在 `module/analysis` 内；后续出现跨业务重复逻辑时再小步抽象。
- 当前只保存结构化 AI 结果和错误信息，不保存原始 AI 输出。

## v2.1 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.1.1 现有 AI 能力检查 | 已完成 |
| v2.1.2 AI 分层结构整理 | 已完成 |
| v2.1.3 Prompt 模板规范 | 已完成 |
| v2.1.4 AI 输出结构与解析规范 | 已完成 |
| v2.1.5 AI 安全规则落地 | 已完成 |
| v2.1.6 联调、审查与日志 | 已完成 |

---

# v2.2 - 岗位描述输入与结构化解析

状态：已完成

## v2.2 总结

v2.2 已完成岗位描述输入与结构化解析闭环：

- 已新增 `job_descriptions` 表迁移脚本 `V8__create_job_descriptions_table.sql`。
- 已新增 `JobDescription` 实体和 `JobDescriptionMapper`。
- 已实现 `POST /api/job-descriptions` 岗位描述提交接口。
- 已实现 `GET /api/job-descriptions` 当前用户岗位描述列表接口。
- 已实现 `GET /api/job-descriptions/{id}` 岗位描述详情接口。
- 已将 `/api/job-descriptions/**` 加入认证保护。
- 已校验标题不能为空、岗位描述原文不能为空，且限制标题和原文最大长度。
- 已保证用户只能查看和解析自己的岗位描述。
- 已创建 `job_description_parse_v1` Prompt，输入为岗位描述原文。
- 已明确 AI 输出字段：`jobTitle`、`requiredSkills`、`bonusSkills`、`experienceSignals`、`responsibilities`、`keywords`、`summary`。
- 已要求 Prompt 不编造岗位描述中不存在的职位、技能、经验、职责或招聘条件。
- 已新增岗位描述结构化解析 Service 和输出 Parser。
- 已实现 AI 调用、JSON 解析、结构化结果保存、模型名称保存和 Prompt 版本保存。
- 已实现 `POST /api/job-descriptions/{id}/parse` 岗位描述解析接口。
- 已处理 AI 调用失败和 AI 返回格式错误，失败时保存 `FAILED` 状态和错误信息，并清空结构化结果。
- 已新增前端岗位描述列表页、输入页和详情页。
- 已接入 `/job-descriptions`、`/job-descriptions/new` 和 `/job-descriptions/:id` 路由，并使用登录守卫保护。
- 已支持前端查看已提交岗位描述、提交岗位描述、触发解析、查看解析状态和结构化解析结果。
- 已展示职位名称、岗位摘要、必备技能、加分技能、经验信号、职责内容和关键词。
- 已修复岗位描述详情页首次进入不加载数据的问题。
- 已将岗位描述解析前端请求超时时间调整为 120 秒。
- 已完成后端相关测试、前端构建和手动联调，用户确认可成功解析。
- 迭代日志已更新：`docs/iteration-log/v2.2-job-description-parse.md`。

说明：

- v2.2 不做简历与岗位 AI 匹配。
- v2.2 不做岗位描述删除功能或复杂岗位管理后台。
- 当前岗位描述解析结果以 JSON 字符串保存在 `structured_content`，后续 v2.3 可直接读取。
- 当前失败状态保存错误信息，不保存原始 AI 输出。

## v2.2 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.2.1 岗位描述表设计 | 已完成 |
| v2.2.2 岗位描述提交接口 | 已完成 |
| v2.2.3 岗位描述解析 Prompt | 已完成 |
| v2.2.4 岗位描述结构化解析服务 | 已完成 |
| v2.2.5 岗位描述解析接口 | 已完成 |
| v2.2.6 前端岗位描述输入与解析展示 | 已完成 |
| v2.2.7 v2.2 联调、审查与日志 | 已完成 |

---

# v2.3 - 简历与岗位 AI 匹配分析

状态：已完成

## v2.3 总结

v2.3 已完成简历与岗位描述的 AI 匹配分析闭环：

- 已新增 `ai_job_match_results` 表迁移脚本 `V9__create_ai_job_match_results_table.sql`。
- 已新增 `AiJobMatchResult` 实体和 `AiJobMatchResultMapper`。
- 已建立 `resume_id`、`job_description_id` 与匹配结果的关联关系，并对同一简历和同一岗位描述设置唯一约束。
- 已明确 AI 匹配结果字段：总体分数、强匹配项、弱匹配项、缺失技能、表达较弱经历、匹配依据、风险提示、模型名称、Prompt 版本和错误信息。
- 已创建 `ai_job_match_v1` Prompt，输入为简历结构化解析结果和岗位描述结构化解析结果。
- 已要求 Prompt 只基于输入内容分析，不编造用户不存在的经历、技能、项目、证书、奖项或量化指标。
- 已新增 AI 匹配 Prompt 构建、输出解析和业务编排服务。
- 已实现 AI 输出 JSON 解析、分数截断、字段兜底和失败状态保存。
- 已实现 `POST /api/resumes/{resumeId}/ai-job-matches`，用于触发当前用户简历与岗位描述的 AI 匹配。
- 已实现 `GET /api/resumes/{resumeId}/ai-job-matches`，支持查询简历下的匹配结果列表，也支持通过 `jobDescriptionId` 查询指定结果。
- 已保证用户只能匹配和查看自己的简历、岗位描述和 AI 匹配结果。
- 已新增前端 AI 匹配页面 `/ai-job-matches`，并通过登录守卫保护。
- 已支持前端选择简历和已解析岗位描述，触发 AI 匹配并展示结构化结果。
- 已展示总体分数、强匹配项、弱匹配项、缺失技能、表达较弱经历、匹配依据、风险提示、模型名称、Prompt 版本和更新时间。
- 已接入首页、岗位描述列表页和岗位描述详情页的 AI 匹配入口。
- 已将 AI 匹配请求超时时间设置为 120 秒，并将默认 `OPENAI_MAX_TOKENS` 调整为 4000。
- 已增强 OpenAI-compatible 客户端对空响应、截断响应和非 JSON 响应的错误提示。
- 已优化 AI 匹配 Prompt 输入长度，降低输出超限和非 JSON 结果概率。
- 已在历史记录中接入 AI 岗位匹配结果，最近匹配可以显示并跳转到 AI 匹配页面。
- 已修复简历删除时未清理 AI 匹配结果导致的删除失败问题。
- 已新增岗位描述删除接口和前端删除入口，删除岗位描述时同步清理关联 AI 匹配结果。
- 已完成后端相关测试、前端构建和本地联调，用户确认 AI 匹配可以成功解析并展示。
- 迭代日志已更新：`docs/iteration-log/v2.3-ai-match.md`。

说明：

- v2.3 不生成最终简历优化方案。
- v2.3 不生成局部改写内容。
- v2.3 不做向量语义匹配、RAG、pgvector 或多轮对话。
- 当前 AI 匹配结果以结构化 JSON 字符串保存，后续 v2.4 可基于成功匹配结果生成优化建议。
- 当前失败状态保存错误信息，不保存原始 AI 输出。

## v2.3 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.3.1 AI 匹配结果表设计 | 已完成 |
| v2.3.2 AI 匹配 Prompt 设计 | 已完成 |
| v2.3.3 AI 匹配分析服务 | 已完成 |
| v2.3.4 AI 匹配触发接口 | 已完成 |
| v2.3.5 AI 匹配结果查询接口 | 已完成 |
| v2.3.6 前端 AI 匹配结果展示 | 已完成 |
| v2.3.7 v2.3 联调、审查与日志 | 已完成 |

---

# v2.4 - AI 简历优化建议增强

状态：已完成

## v2.4 总结

v2.4 已完成基于 AI 匹配结果的简历优化建议闭环：

- 已新增 `ai_resume_suggestions` 表迁移脚本 `V10__create_ai_resume_suggestions_table.sql`。
- 已新增 `AiResumeSuggestion` 实体和 `AiResumeSuggestionMapper`。
- 已建立 `resume_id`、`job_description_id`、`ai_job_match_result_id` 与优化建议结果的关联关系，并对同一 AI 匹配结果设置唯一约束。
- 已明确优化建议状态、结构化建议 JSON、模型名称、Prompt 版本、错误信息和更新时间字段。
- 已创建 `resume_suggestion_v1` Prompt，输入为简历结构化解析结果、岗位描述结构化解析结果和 AI 匹配结果。
- Prompt 要求只基于输入内容提出建议，不编造用户不存在的经历、技能、证书、奖项或量化指标。
- 已新增优化建议 Prompt 构建、输出解析和业务编排服务。
- 已实现建议类型、优先级、问题、建议内容、依据、注意事项和关联项的结构化解析与校验。
- 已实现 `POST /api/resumes/{resumeId}/ai-suggestions`，用于基于成功 AI 匹配结果触发优化建议生成。
- 已实现 `GET /api/resumes/{resumeId}/ai-suggestions`，支持列表查询，也支持通过 `jobDescriptionId` 或 `aiJobMatchResultId` 查询指定建议结果。
- 已保证用户只能基于自己的简历、岗位描述和 AI 匹配结果生成或查看优化建议。
- 已明确重复生成策略：同一 `ai_job_match_result_id` 再次生成时覆盖旧建议结果。
- AI 调用失败或输出解析失败时保存 `FAILED` 状态和错误信息，不保存脏建议。
- 已新增前端优化建议 API 和类型定义。
- 已在 AI 匹配页面增加优化建议生成入口和结果展示区域。
- 前端已展示建议状态、建议数量、建议类型、优先级、目标简历部分、问题、建议内容、依据、注意事项、模型名称和生成时间。
- 前端已按高优先级、技能缺口、经历表达、优势与综合建议分组展示。
- 已增加“AI 建议需用户确认，不应直接伪造经历”的前端提示。
- 已完成后端相关测试、前端构建和本地手动联调，用户确认优化建议可以成功生成并展示。
- 迭代日志已更新：`docs/iteration-log/v2.4-ai-suggestions.md`。

说明：

- v2.4 不生成完整定制版简历。
- v2.4 不生成局部改写文本，局部改写进入 v2.5。
- v2.4 不做 RAG、pgvector 或多轮对话。
- 当前建议结果以结构化 JSON 字符串保存在 `suggestions` 字段。
- 当前失败状态保存错误信息，不保存原始 AI 输出。

## v2.4 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.4.1 优化建议结果结构设计 | 已完成 |
| v2.4.2 优化建议 Prompt 设计 | 已完成 |
| v2.4.3 优化建议生成服务 | 已完成 |
| v2.4.4 优化建议触发接口 | 已完成 |
| v2.4.5 优化建议查询接口 | 已完成 |
| v2.4.6 前端优化建议展示 | 已完成 |
| v2.4.7 v2.4 联调、审查与日志 | 已完成 |

---

# v2.5 - AI 局部改写建议

状态：已完成

## v2.5 总结

v2.5 已完成 AI 局部改写建议闭环：

- 已新增 `ai_rewrite_suggestions` 表迁移脚本、实体和 Mapper。
- 已建立局部改写建议与简历、岗位描述、AI 匹配结果、AI 优化建议的可选关联关系。
- 已明确改写对象类型、目标简历部分、原文、改写建议、改写理由、注意事项、生成状态、采纳状态、模型名称、Prompt 版本和错误信息字段。
- 已创建 `rewrite_suggestion_v1` Prompt，输入为原文片段、改写对象类型、目标简历部分，以及可选岗位描述结构化结果、AI 匹配结果和 AI 优化建议。
- Prompt 明确要求只基于用户已有内容进行表达优化，不编造项目经历、技术栈、证书、奖项或虚假量化指标。
- 已新增局部改写 Prompt 构建、输出解析和业务编排服务。
- 已实现 AI 输出 JSON 解析与校验，格式异常时保存 `FAILED` 状态，不保存脏改写正文。
- 已实现 `POST /api/resumes/{resumeId}/rewrite-suggestions`，用于生成局部改写建议。
- 已实现 `GET /api/resumes/{resumeId}/rewrite-suggestions`，支持按简历查询局部改写建议，并支持按改写类型和采纳状态筛选。
- 已实现 `PATCH /api/rewrite-suggestions/{rewriteId}/accept-status`，用于记录用户采纳或拒绝改写建议。
- 已保证用户只能生成、查看和更新自己的局部改写建议。
- 已新增前端局部改写 API 和类型定义。
- 已在 AI 匹配页面增加局部改写弹窗、原文与改写建议对比展示、改写理由、注意事项、历史改写建议列表和采纳/拒绝按钮。
- 已修复采纳/拒绝接口的 `PATCH` CORS 配置问题，并将 `/api/rewrite-suggestions/**` 纳入鉴权路径。
- 已完成后端相关测试、前端构建和本地手动联调，用户确认局部改写生成、查询、采纳和拒绝流程可用。
- 迭代日志已更新：`docs/iteration-log/v2.5-rewrite.md`。

说明：

- v2.5 不自动修改原始简历文件。
- v2.5 不生成完整定制版简历。
- v2.5 不导出 PDF 或 DOCX。
- v2.5 不做 RAG、pgvector 或多轮对话。
- 当前采纳状态只记录用户决策，不会自动覆盖简历内容。
- 当前事实一致性主要依赖 Prompt 约束、输出结构校验和用户确认，后续可结合评估流程继续增强。

## v2.5 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.5.1 局部改写结果表设计 | 已完成 |
| v2.5.2 局部改写 Prompt 设计 | 已完成 |
| v2.5.3 局部改写服务 | 已完成 |
| v2.5.4 局部改写接口 | 已完成 |
| v2.5.5 前端原文与改写建议对比展示 | 已完成 |
| v2.5.6 用户确认状态记录 | 已完成 |
| v2.5.7 v2.5 联调、审查与日志 | 已完成 |

---

# v2.5.5 - 产品信息架构与模块边界收敛

状态：已完成

## v2.5.5 总结

v2.5.5 已完成产品结构收敛：

- 已梳理当前普通用户侧、管理员侧、前端页面、主要后端接口、首页入口、简历入口、岗位入口和 AI 入口。
- 已新增并更新 `docs/product-structure.md`，记录模块边界、岗位边界、AI 功能边界、用户主流程、导航结构、页面命名和 v2.6 AI 历史聚合范围。
- 已新增并更新 `docs/page-list.md`，记录当前页面、推荐页面名称、页面类型、主导航建议、详情页/结果区块、页面跳转关系和管理员专属页面边界。
- 已将 AI 能力边界收敛为：简历诊断、目标岗位解析、匹配分析、岗位优化建议、AI 局部改写、AI 历史。
- 已将岗位相关概念收敛为：岗位库、目标岗位、岗位管理；普通用户侧不再使用“岗位管理”命名。
- 已将普通用户主流程收敛为：工作台 -> 我的简历 -> 目标岗位 -> 匹配与优化 -> AI 历史。
- 已完成前端轻量入口调整，将页面标题和关键按钮文案统一到新的产品语义，保留原有路由路径。
- 已确认 v2.6 AI 历史聚合类型：`RESUME_DIAGNOSIS`、`TARGET_JOB_PARSE`、`MATCH_ANALYSIS`、`JOB_OPTIMIZATION_SUGGESTION`、`LOCAL_REWRITE`。
- 已创建迭代日志：`docs/iteration-log/v2.5.5-product-structure.md`。
- 已执行 `cd web && npm run build`，前端构建通过。

说明：

- v2.5.5 不新增 AI 生成能力。
- v2.5.5 不实现 AI 历史接口或 AI 历史页面。
- v2.5.5 不修改数据库结构。
- v2.5.5 不重构后端接口路径。
- v2.5.5 不做大规模 UI 重写，只做前端入口、页面标题和按钮文案的轻量调整。

## v2.5.5 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.5.5.1 当前功能模块梳理 | 已完成 |
| v2.5.5.2 AI 功能边界重新定义 | 已完成 |
| v2.5.5.3 岗位相关功能边界收敛 | 已完成 |
| v2.5.5.4 用户主流程重新设计 | 已完成 |
| v2.5.5.5 导航结构重新设计 | 已完成 |
| v2.5.5.6 页面命名与页面清单更新 | 已完成 |
| v2.5.5.7 前端入口轻量调整 | 已完成 |
| v2.5.5.8 v2.6 AI 历史聚合范围确认 | 已完成 |
| v2.5.5.9 文档更新与迭代日志 | 已完成 |

---

# v2.6 - AI 结果持久化与历史回看

状态：已完成

## v2.6 总结

v2.6 已完成 AI 结果持久化与历史回看闭环。系统现在可以统一回看简历诊断、目标岗位解析、匹配分析、岗位优化建议和局部改写结果，并支持按类型、简历、目标岗位和状态筛选。

本版本保持边界：不新增 AI 生成能力，不引入评估流程，不做 Embedding、pgvector、RAG、多轮对话或完整简历生成。

完成内容：

- 梳理并统一五类 AI 结果元数据：模型名称、Prompt 版本、状态、错误信息、创建时间和更新时间。
- 明确 AI 历史聚合类型：`RESUME_DIAGNOSIS`、`TARGET_JOB_PARSE`、`MATCH_ANALYSIS`、`JOB_OPTIMIZATION_SUGGESTION`、`LOCAL_REWRITE`。
- 采用聚合查询方案，不新建 `ai_result_records` 表，不重复存储大段 AI 输出内容。
- 新增 `/api/ai-results` 列表接口，支持分页和按类型、简历、目标岗位、状态筛选。
- 新增 `/api/ai-results/{resultType}/{recordId}` 详情接口，按 `resultType + recordId` 查询来源表详情。
- 将 `/api/ai-results/**` 纳入鉴权，保证用户只能查看自己的 AI 结果。
- 将前端 `/history` 升级为 AI 历史页面，支持列表、筛选、分页、详情抽屉和业务跳转。
- 前后端均避免展示 API Key、完整 Prompt 等敏感信息。

## v2.6 小任务完成情况

| 小任务 | 状态 | 说明 |
|---|---|---|
| v2.6.1 AI 结果元数据统一 | 已完成 | 已确认五类 AI 结果均可追踪模型、Prompt、状态、错误信息和时间，并补齐详情 VO 的 `createdAt`。 |
| v2.6.2 AI 历史记录聚合查询设计 | 已完成 | 已明确采用聚合查询方案，不新建统一流水表，保留旧 `/api/history` 简历维度历史接口。 |
| v2.6.3 AI 结果列表接口 | 已完成 | 已实现 `GET /api/ai-results`，聚合五类 AI 结果并支持分页和筛选。 |
| v2.6.4 AI 结果详情接口 | 已完成 | 已实现 `GET /api/ai-results/{resultType}/{recordId}`，支持五类 AI 结果详情回看。 |
| v2.6.5 前端 AI 历史页面 | 已完成 | 已将 `/history` 升级为 AI 历史页面，接入列表和详情接口。 |
| v2.6.6 v2.6 联调、审查与日志 | 已完成 | 已完成本地联调确认，并更新任务清单与迭代日志。 |


# v2.7 - AI 输出评估流程

状态：当前版本

## v2.7 目标

建立基础 AI 输出质量评估流程，避免 Prompt 修改后质量不可控。

v2.7 只负责“评估集、评估维度、输出记录、人工评分和 Prompt 修改规则”，不负责新增新的 AI 业务功能，不负责模型微调，不负责复杂自动化评测平台。

v2.7 完成后应满足：

- 项目中有明确的 AI 输出评估目录。
- 至少准备 3 份样例简历。
- 至少准备 3 份样例岗位描述。
- 至少覆盖简历分析、岗位描述解析、AI 匹配、优化建议、局部改写中的部分核心能力。
- 每个评估样例有输入、输出和人工评价记录。
- 评估维度包括相关性、事实一致性、具体程度、实用性、结构稳定性、可解释性和安全性。
- Prompt 修改前后可以对比输出效果。
- AI 输出问题可以被记录并反馈到 Prompt 修改规则中。
- 本轮结果记录到 `docs/iteration-log/v2.7-ai-evaluation.md`。

说明：

- v2.7 不做企业级评测平台。
- v2.7 不做模型微调。
- v2.7 不做自动打分系统。
- v2.7 不做大规模测试集。
- v2.7 不引入复杂 A/B 测试。
- v2.7 的重点是建立一个轻量、可持续、能辅助 Prompt 迭代的小型评估流程。

---

## v2.7.1 评估集目录设计

状态：当前任务

目标：

- 设计 AI 输出评估相关目录。
- 明确样例简历、样例岗位描述、AI 输出记录、人工评估记录的存放位置。
- 让后续 Prompt 修改和 AI 输出对比有固定记录位置。

任务：

- [ ] 创建 `docs/evaluation/` 目录。
- [ ] 创建 `docs/evaluation/resumes/` 目录。
- [ ] 创建 `docs/evaluation/job-descriptions/` 目录。
- [ ] 创建 `docs/evaluation/outputs/` 目录。
- [ ] 创建 `docs/evaluation/reports/` 目录。
- [ ] 创建 `docs/evaluation/evaluation-notes.md`。
- [ ] 创建评估集说明文件。
- [ ] 明确样例文件命名规则。
- [ ] 明确 AI 输出结果文件命名规则。
- [ ] 明确评估报告文件命名规则。
- [ ] 明确哪些内容可以提交到仓库。
- [ ] 明确哪些内容不能包含真实隐私数据。

建议目录结构：

```text
docs/evaluation/
├── README.md
├── resumes/
│   ├── resume-sample-001.md
│   ├── resume-sample-002.md
│   └── resume-sample-003.md
├── job-descriptions/
│   ├── jd-sample-001.md
│   ├── jd-sample-002.md
│   └── jd-sample-003.md
├── outputs/
│   ├── resume-analysis/
│   ├── job-description-parse/
│   ├── ai-job-match/
│   ├── ai-suggestions/
│   └── rewrite-suggestions/
├── reports/
│   └── evaluation-report-v1.md
└── evaluation-notes.md
```

命名规则建议：

| 类型 | 命名示例 |
|---|---|
| 样例简历 | `resume-sample-001.md` |
| 样例岗位描述 | `jd-sample-001.md` |
| AI 输出 | `resume-sample-001_jd-sample-001_prompt-v1.md` |
| 评估报告 | `evaluation-report-v1.md` |

验收标准：

- 评估目录结构明确。
- 样例简历、岗位描述、输出、报告有独立目录。
- 文件命名规则明确。
- 隐私数据处理规则明确。
- 不放入真实用户简历隐私数据。
- 不实现自动化评估代码。
- 不新增 AI 业务接口。

---

## v2.7.2 样例简历准备

状态：未开始

目标：

- 准备用于评估 AI 输出质量的样例简历。
- 样例简历应覆盖不同能力水平和不同目标岗位方向。
- 样例内容不能包含真实个人隐私信息。

任务：

- [ ] 准备至少 3 份样例简历。
- [ ] 样例简历 1：Java 后端实习方向。
- [ ] 样例简历 2：AI 应用开发方向。
- [ ] 样例简历 3：数据分析或算法实习方向。
- [ ] 每份简历包含基础信息。
- [ ] 每份简历包含教育经历。
- [ ] 每份简历包含技能列表。
- [ ] 每份简历包含项目经历。
- [ ] 可选包含实习经历。
- [ ] 可选包含获奖经历。
- [ ] 为每份简历标注预期优势。
- [ ] 为每份简历标注预期问题。
- [ ] 确认样例简历不包含真实手机号、邮箱、身份证号等隐私信息。

样例简历建议字段：

| 字段 | 说明 |
|---|---|
| candidateProfile | 候选人背景 |
| targetDirection | 目标方向 |
| education | 教育经历 |
| skills | 技能 |
| projects | 项目经历 |
| internships | 实习经历，可选 |
| awards | 奖项，可选 |
| expectedStrengths | 预期优势 |
| expectedWeaknesses | 预期问题 |

验收标准：

- 至少有 3 份样例简历。
- 样例简历方向不同。
- 样例简历内容足够支撑 AI 分析和匹配。
- 样例简历包含预期优势和预期问题。
- 样例简历不包含真实个人隐私数据。
- 样例文件放在 `docs/evaluation/resumes/`。
- 本任务不调用 AI。

---

## v2.7.3 样例岗位描述准备

状态：未开始

目标：

- 准备用于评估 AI 匹配和优化建议的样例岗位描述。
- 岗位描述应覆盖不同方向和不同技能要求。
- 岗位描述内容可以基于公开招聘描述改写，但不要直接复制敏感或版权内容。

任务：

- [ ] 准备至少 3 份样例岗位描述。
- [ ] 样例岗位 1：Java 后端实习生。
- [ ] 样例岗位 2：AI 应用开发实习生。
- [ ] 样例岗位 3：数据分析或算法实习生。
- [ ] 每份岗位描述包含岗位名称。
- [ ] 每份岗位描述包含岗位职责。
- [ ] 每份岗位描述包含必备技能。
- [ ] 每份岗位描述包含加分技能。
- [ ] 每份岗位描述包含经验信号。
- [ ] 为每份岗位描述标注预期关键词。
- [ ] 为每份岗位描述标注匹配关注点。
- [ ] 确认岗位描述没有直接使用真实公司敏感信息。

样例岗位描述建议字段：

| 字段 | 说明 |
|---|---|
| jobTitle | 岗位名称 |
| jobDirection | 岗位方向 |
| responsibilities | 岗位职责 |
| requiredSkills | 必备技能 |
| bonusSkills | 加分技能 |
| experienceSignals | 经验信号 |
| expectedKeywords | 预期关键词 |
| matchingFocus | 匹配关注点 |

验收标准：

- 至少有 3 份样例岗位描述。
- 岗位方向覆盖不同目标。
- 岗位描述足够支撑 AI 匹配和建议生成。
- 每份岗位描述包含预期关键词和匹配关注点。
- 样例文件放在 `docs/evaluation/job-descriptions/`。
- 本任务不调用 AI。

---

## v2.7.4 评估维度与评分表设计

状态：未开始

目标：

- 设计 AI 输出质量评估维度。
- 建立人工评分表。
- 让不同 Prompt 版本的输出可以进行基本对比。

任务：

- [ ] 明确评估维度。
- [ ] 为每个维度定义评分标准。
- [ ] 设计 1 到 5 分评分规则。
- [ ] 明确每个维度的扣分标准。
- [ ] 明确严重问题判定标准。
- [ ] 设计评估记录模板。
- [ ] 设计汇总报告模板。
- [ ] 将模板写入 `docs/evaluation/evaluation-notes.md`。
- [ ] 将汇总模板写入 `docs/evaluation/reports/evaluation-report-v1.md`。

建议评估维度：

| 维度 | 说明 |
|---|---|
| relevance | 相关性：是否围绕简历和岗位 |
| factualConsistency | 事实一致性：是否没有编造经历 |
| specificity | 具体程度：建议是否具体 |
| usefulness | 实用性：是否有助于简历优化 |
| structureStability | 结构稳定性：是否符合 JSON 结构 |
| explainability | 可解释性：结论是否有依据 |
| safety | 安全性：是否避免虚假、敏感或误导内容 |

建议评分规则：

| 分数 | 含义 |
|---|---|
| 5 | 表现优秀，可以直接采用 |
| 4 | 表现较好，只需轻微调整 |
| 3 | 基本可用，但存在明显改进空间 |
| 2 | 问题较多，需要修改 Prompt |
| 1 | 不可用，存在严重错误 |

严重问题示例：

| 问题 | 说明 |
|---|---|
| 编造经历 | 生成用户不存在的项目、实习或技能 |
| 结构错误 | 输出无法被系统解析 |
| 建议空泛 | 建议无法指导实际修改 |
| 依据缺失 | 匹配结论没有说明来源 |
| 安全风险 | 暴露敏感信息或误导用户 |

验收标准：

- 评估维度明确。
- 每个维度有评分规则。
- 有人工评估记录模板。
- 有汇总报告模板。
- 明确严重问题判定标准。
- 本任务不调用 AI。
- 本任务不实现自动化评分。

---

## v2.7.5 Prompt 输出结果记录

状态：未开始

目标：

- 使用样例简历和样例岗位描述，记录不同 Prompt 版本的 AI 输出结果。
- 为后续人工评估和 Prompt 调整提供依据。

任务：

- [ ] 选择需要评估的 Prompt。
- [ ] 记录 Prompt 版本。
- [ ] 选择样例简历。
- [ ] 选择样例岗位描述。
- [ ] 运行岗位描述解析输出。
- [ ] 运行简历 AI 分析输出。
- [ ] 运行 AI 岗位匹配输出。
- [ ] 运行 AI 优化建议输出。
- [ ] 运行局部改写输出。
- [ ] 保存每次 AI 输出结果。
- [ ] 记录模型名称。
- [ ] 记录生成时间。
- [ ] 记录输入样例编号。
- [ ] 记录输出是否解析成功。
- [ ] 记录明显异常。

建议输出记录字段：

| 字段 | 说明 |
|---|---|
| sampleResumeId | 样例简历编号 |
| sampleJobDescriptionId | 样例岗位描述编号 |
| promptName | Prompt 名称 |
| promptVersion | Prompt 版本 |
| modelName | 模型名称 |
| outputTime | 输出时间 |
| parseSuccess | 是否解析成功 |
| rawOutput | 原始输出 |
| structuredOutput | 结构化输出 |
| notes | 备注 |

输出保存路径建议：

```text
docs/evaluation/outputs/
```

验收标准：

- 至少记录 3 组 AI 输出结果。
- 输出结果能对应样例简历和样例岗位描述。
- 输出结果能对应 Prompt 版本。
- 输出结果能对应模型名称。
- 解析失败结果也要记录。
- 不记录真实用户隐私数据。
- 本任务不修改业务代码。

---

## v2.7.6 评估问题汇总与 Prompt 修改规则

状态：未开始

目标：

- 对已记录的 AI 输出进行人工评估。
- 汇总常见问题。
- 形成 Prompt 修改规则，指导后续迭代。

任务：

- [ ] 使用评分表评估 AI 输出。
- [ ] 给每个输出记录各维度评分。
- [ ] 标记是否存在严重问题。
- [ ] 汇总结构错误问题。
- [ ] 汇总事实不一致问题。
- [ ] 汇总建议空泛问题。
- [ ] 汇总依据不足问题。
- [ ] 汇总安全风险问题。
- [ ] 总结 Prompt 修改方向。
- [ ] 形成 Prompt 修改规则。
- [ ] 更新 `docs/evaluation/evaluation-notes.md`。
- [ ] 更新 `docs/evaluation/reports/evaluation-report-v1.md`。

建议问题分类：

| 问题类型 | 说明 |
|---|---|
| FORMAT_ERROR | 输出格式错误 |
| HALLUCINATION | 编造经历或事实 |
| VAGUE_SUGGESTION | 建议空泛 |
| MISSING_EVIDENCE | 缺少依据 |
| OVER_OPTIMIZATION | 改写过度 |
| UNSAFE_CONTENT | 存在安全风险 |
| LOW_RELEVANCE | 与岗位或简历相关性低 |

Prompt 修改规则示例：

- 如果输出经常编造经历，在 Prompt 中进一步强调“只能基于输入内容”。
- 如果建议空泛，要求输出必须包含目标简历部分、问题、修改建议和依据。
- 如果结构不稳定，减少自由文本，强化 JSON 示例。
- 如果匹配依据不足，要求每个结论至少引用一条简历证据或岗位证据。
- 如果改写过度，要求保留事实边界并输出 caution。

验收标准：

- 至少完成 3 组 AI 输出的人工评估。
- 每组评估有维度评分。
- 常见问题已分类。
- Prompt 修改规则已记录。
- 评估报告已更新。
- 不实现复杂自动化评估平台。
- 不做模型微调。

---

## v2.7.7 v2.7 日志与总结

状态：未开始

目标：

- 完成 v2.7 的评估流程收口。
- 更新迭代日志。
- 为 v2.8 的 Embedding / pgvector / RAG 初步增强做准备。

任务：

- [ ] 检查评估目录是否完整。
- [ ] 检查样例简历是否准备完成。
- [ ] 检查样例岗位描述是否准备完成。
- [ ] 检查评估维度和评分表是否完成。
- [ ] 检查 Prompt 输出记录是否完成。
- [ ] 检查评估报告是否完成。
- [ ] 检查 Prompt 修改规则是否记录。
- [ ] 更新 `docs/iteration-log/v2.7-ai-evaluation.md`。
- [ ] 更新 `docs/tasks/phase-3-task-list.md`。
- [ ] 准备进入 v2.8。

验收标准：

- v2.7 AI 输出评估流程可执行。
- 至少有 3 份样例简历。
- 至少有 3 份样例岗位描述。
- 至少有 3 组 AI 输出记录。
- 至少有 3 组人工评估结果。
- 评估报告已更新。
- Prompt 修改规则已记录。
- v2.7 迭代日志已更新。

---

## v2.7 禁止提前实现内容

在 v2.7 中不允许提前实现：

- Embedding。
- pgvector。
- RAG。
- 多轮对话。
- 职业顾问聊天。
- 全自动职位投递。
- 模型微调。
- 企业级 Prompt 实验平台。
- 复杂 A/B 测试平台。
- 自动化评测平台。
- 自动打分模型训练。
- 微服务拆分。
- 生产级监控系统。

---

## v2.7 完成后的下一步

v2.7 完成后，进入：

`v2.8 - Embedding / pgvector / RAG 初步增强`

v2.8 再开始处理：

- pgvector 可行性检查。
- 向量表设计。
- Embedding 模型接入。
- 简历向量生成。
- 岗位描述向量生成。
- 基础语义相似度查询。
- 简单 RAG 检索增强。

# v2.8 - Embedding / pgvector / RAG 初步增强

状态：当前版本

## v2.8 目标

在 AI 输出结构和评估流程稳定后，尝试引入基础语义匹配或检索增强能力，为后续更高级 AI 能力打基础。

v2.8 只负责“基础向量化、基础语义相似度查询、简单 RAG 检索增强”，不负责复杂推荐系统、不负责企业级知识库、不负责多轮对话和微服务拆分。

v2.8 完成后应满足：

- 系统可以接入 Embedding 模型。
- 系统可以为简历文本生成向量。
- 系统可以为岗位描述生成向量。
- 系统可以将向量保存到 PostgreSQL + pgvector。
- 系统可以计算简历与岗位描述之间的基础语义相似度。
- 语义相似度结果可以辅助岗位匹配分析。
- 系统可以基于简历片段进行简单检索增强。
- RAG 只作为辅助上下文，不替代原有 AI 匹配和优化建议流程。
- 用户只能检索和使用自己的简历、岗位描述和向量数据。
- 本轮结果记录到 `docs/iteration-log/v2.8-vector-rag.md`。

说明：

- v2.8 不做复杂推荐系统。
- v2.8 不做大规模向量数据库平台。
- v2.8 不做企业级知识库。
- v2.8 不做多轮对话。
- v2.8 不做职业顾问聊天。
- v2.8 不做自动职位投递。
- v2.8 不做模型微调。
- v2.8 不做微服务拆分。
- v2.8 的重点是建立“可运行、可解释、可扩展”的基础语义匹配能力。

---

## v2.8.1 pgvector 可行性检查

状态：当前任务

目标：

- 确认当前 PostgreSQL 环境是否可以启用 pgvector。
- 明确本地开发、测试环境和后续部署环境对 pgvector 的支持方式。
- 明确 pgvector 接入风险和降级策略。

任务：

- [ ] 检查当前 PostgreSQL 版本。
- [ ] 检查是否已安装 pgvector 扩展。
- [ ] 检查 Docker / Podman 环境是否可以使用带 pgvector 的 PostgreSQL 镜像。
- [ ] 确认本地数据库是否可以执行 `CREATE EXTENSION vector;`。
- [ ] 确认 Flyway 是否可以管理 pgvector 扩展初始化。
- [ ] 明确向量维度来源。
- [ ] 明确 Embedding 模型输出维度。
- [ ] 明确如果 pgvector 不可用时的临时降级方案。
- [ ] 更新 README 或环境说明。
- [ ] 更新 `docs/iteration-log/v2.8-vector-rag.md`。

建议检查项：

| 检查项 | 说明 |
|---|---|
| PostgreSQL 版本 | 建议使用支持 pgvector 的稳定版本 |
| pgvector 扩展 | 是否可安装并启用 |
| Docker / Podman 镜像 | 是否有可用 pgvector 镜像 |
| Flyway 初始化 | 是否可以自动创建扩展 |
| 向量维度 | 是否与 Embedding 模型一致 |

建议降级策略：

- 如果 pgvector 暂时不可用，可以先保存 Embedding JSON 字符串。
- 如果 Embedding 模型暂时不可用，可以先保留接口和表结构，不执行真实向量生成。
- 降级方案只能用于开发过渡，不作为最终目标。

验收标准：

- pgvector 可行性明确。
- 本地环境是否支持 pgvector 已确认。
- 向量维度来源明确。
- 是否通过 Flyway 创建扩展已明确。
- 不实现向量表。
- 不实现 Embedding 调用。
- 不实现 RAG。
- 本任务只做可行性检查和环境确认。

---

## v2.8.2 向量表设计

状态：未开始

目标：

- 设计简历向量表和岗位描述向量表。
- 明确向量与原始简历、岗位描述、文本片段之间的关联关系。
- 明确向量维度、模型名称、文本片段、创建时间等字段。

任务：

- [ ] 设计 `resume_embeddings` 表。
- [ ] 设计 `job_description_embeddings` 表。
- [ ] 明确 `resume_id` 与 `resumes.id` 的关联关系。
- [ ] 明确 `job_description_id` 与 `job_descriptions.id` 的关联关系。
- [ ] 明确文本片段字段 `chunk_text`。
- [ ] 明确片段序号字段 `chunk_index`。
- [ ] 明确向量字段 `embedding`。
- [ ] 明确向量模型名称字段 `embedding_model`。
- [ ] 明确向量维度字段 `embedding_dimension`。
- [ ] 明确生成状态字段 `embedding_status`。
- [ ] 明确错误信息字段 `error_message`。
- [ ] 明确创建时间字段 `created_at`。
- [ ] 明确更新时间字段 `updated_at`。
- [ ] 创建 pgvector 扩展初始化脚本。
- [ ] 创建数据库迁移脚本。
- [ ] 创建向量实体和 Mapper。
- [ ] 明确是否需要建立向量索引。

建议 `resume_embeddings` 字段：

| 字段名 | 说明 |
|---|---|
| id | 主键 |
| resume_id | 关联简历 ID |
| user_id | 用户 ID |
| chunk_index | 文本片段序号 |
| chunk_text | 文本片段 |
| embedding | 向量字段 |
| embedding_model | Embedding 模型名称 |
| embedding_dimension | 向量维度 |
| embedding_status | 生成状态 |
| error_message | 错误信息 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

建议 `job_description_embeddings` 字段：

| 字段名 | 说明 |
|---|---|
| id | 主键 |
| job_description_id | 关联岗位描述 ID |
| user_id | 用户 ID |
| chunk_index | 文本片段序号 |
| chunk_text | 文本片段 |
| embedding | 向量字段 |
| embedding_model | Embedding 模型名称 |
| embedding_dimension | 向量维度 |
| embedding_status | 生成状态 |
| error_message | 错误信息 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

建议状态：

| 状态 | 说明 |
|---|---|
| PENDING | 待生成 |
| SUCCESS | 生成成功 |
| FAILED | 生成失败 |

验收标准：

- 简历向量表结构明确。
- 岗位描述向量表结构明确。
- 向量与用户、简历、岗位描述的关系明确。
- 向量维度和模型名称可追踪。
- 实体与表字段对应。
- Mapper 可以被 MyBatis-Plus 扫描。
- 不实现 Embedding 调用。
- 不实现语义查询。
- 不实现 RAG。

---

## v2.8.3 Embedding 模型接入

状态：未开始

目标：

- 接入 Embedding 模型。
- 建立统一 Embedding 调用服务。
- 支持将文本转换为向量。
- 处理 Embedding 调用失败、超时和维度不一致问题。

任务：

- [ ] 确认使用的 Embedding 模型。
- [ ] 明确 Embedding 模型输出维度。
- [ ] 配置 Embedding 模型名称。
- [ ] 配置 Embedding API Key。
- [ ] 配置 Embedding base-url，如使用兼容接口。
- [ ] 创建 `EmbeddingClientService`。
- [ ] 实现文本转向量方法。
- [ ] 校验向量维度。
- [ ] 处理空文本情况。
- [ ] 处理文本过长情况。
- [ ] 处理 API 调用失败。
- [ ] 处理 API 超时。
- [ ] 避免在代码中硬编码 API Key。
- [ ] 将敏感配置放入环境变量或本地配置文件。
- [ ] 补充配置示例。

建议接口职责：

| 服务 | 作用 |
|---|---|
| EmbeddingClientService | 调用外部 Embedding 模型 |
| EmbeddingProperties | 管理模型名称、维度、base-url 等配置 |
| TextChunkService | 负责文本切分，可在 v2.8.4 实现 |

验收标准：

- 后端可以调用 Embedding 模型并获得向量。
- 向量维度与配置一致。
- API Key 不硬编码在 Java 代码中。
- Embedding 调用失败时返回明确错误。
- Embedding 服务与业务逻辑解耦。
- 本任务不保存向量到数据库。
- 本任务不实现语义匹配。
- 本任务不实现 RAG。

---

## v2.8.4 简历向量生成

状态：未开始

目标：

- 基于简历解析结果生成简历文本片段。
- 调用 Embedding 服务生成简历向量。
- 将简历向量保存到 `resume_embeddings` 表。

任务：

- [ ] 创建简历文本切分服务。
- [ ] 从简历解析结果中读取 `raw_text` 或 `structured_content`。
- [ ] 明确切分策略。
- [ ] 生成文本片段 `chunk_text`。
- [ ] 为每个片段生成 `chunk_index`。
- [ ] 调用 Embedding 服务生成向量。
- [ ] 保存向量到 `resume_embeddings` 表。
- [ ] 保存模型名称和向量维度。
- [ ] 保存生成状态。
- [ ] 处理空文本情况。
- [ ] 处理 Embedding 调用失败。
- [ ] 明确重复生成策略。
- [ ] 实现 `POST /api/resumes/{resumeId}/embeddings`。
- [ ] 实现 `GET /api/resumes/{resumeId}/embeddings`。

建议切分策略：

- Phase 3 先采用简单段落切分。
- 每个片段长度控制在模型允许范围内。
- 优先按项目经历、技能、实习经历、教育经历等结构化字段切分。
- 如果结构化字段不足，可以按原始文本段落切分。

重复生成策略：

- Phase 3 默认覆盖同一简历旧向量。
- 重新生成时先删除旧向量，再写入新向量。
- 如果生成失败，应记录失败状态和错误信息。

建议 API：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/resumes/{resumeId}/embeddings` | 生成简历向量 |
| GET | `/api/resumes/{resumeId}/embeddings` | 查询简历向量生成状态 |

验收标准：

- 已解析简历可以生成向量。
- 未解析简历不能直接生成向量。
- 用户不能为他人的简历生成向量。
- 向量可以保存到数据库。
- 模型名称和向量维度可以追踪。
- 生成失败时记录错误信息。
- 重复生成策略明确。
- 本任务不实现岗位描述向量。
- 本任务不实现语义相似度查询。
- 本任务不实现 RAG。

---

## v2.8.5 岗位描述向量生成

状态：未开始

目标：

- 基于岗位描述解析结果生成岗位文本片段。
- 调用 Embedding 服务生成岗位描述向量。
- 将岗位描述向量保存到 `job_description_embeddings` 表。

任务：

- [ ] 从岗位描述解析结果中读取 `raw_text` 或 `structured_content`。
- [ ] 明确岗位描述切分策略。
- [ ] 生成岗位文本片段 `chunk_text`。
- [ ] 为每个片段生成 `chunk_index`。
- [ ] 调用 Embedding 服务生成向量。
- [ ] 保存向量到 `job_description_embeddings` 表。
- [ ] 保存模型名称和向量维度。
- [ ] 保存生成状态。
- [ ] 处理空文本情况。
- [ ] 处理 Embedding 调用失败。
- [ ] 明确重复生成策略。
- [ ] 实现 `POST /api/job-descriptions/{jobDescriptionId}/embeddings`。
- [ ] 实现 `GET /api/job-descriptions/{jobDescriptionId}/embeddings`。

建议切分策略：

- 优先按必备技能、加分技能、职责内容、经验信号切分。
- 如果结构化字段不足，可以按原始岗位描述段落切分。
- 保留原始 chunk_text 便于后续解释语义匹配依据。

重复生成策略：

- Phase 3 默认覆盖同一岗位描述旧向量。
- 重新生成时先删除旧向量，再写入新向量。
- 如果生成失败，应记录失败状态和错误信息。

建议 API：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/job-descriptions/{jobDescriptionId}/embeddings` | 生成岗位描述向量 |
| GET | `/api/job-descriptions/{jobDescriptionId}/embeddings` | 查询岗位描述向量生成状态 |

验收标准：

- 已解析岗位描述可以生成向量。
- 未解析岗位描述不能直接生成向量。
- 用户不能为他人的岗位描述生成向量。
- 向量可以保存到数据库。
- 模型名称和向量维度可以追踪。
- 生成失败时记录错误信息。
- 重复生成策略明确。
- 本任务不实现语义相似度查询。
- 本任务不实现 RAG。

---

## v2.8.6 基础语义相似度查询

状态：未开始

目标：

- 基于简历向量和岗位描述向量计算基础语义相似度。
- 将语义相似度作为 AI 匹配的辅助参考。
- 返回可解释的相似片段结果。

任务：

- [ ] 确认 pgvector 相似度计算方式。
- [ ] 实现简历向量与岗位描述向量相似度查询。
- [ ] 支持查询 Top-K 相似片段。
- [ ] 返回简历片段。
- [ ] 返回岗位描述片段。
- [ ] 返回相似度分数。
- [ ] 校验当前用户是否拥有简历和岗位描述。
- [ ] 校验简历向量是否已生成。
- [ ] 校验岗位描述向量是否已生成。
- [ ] 实现 `GET /api/resumes/{resumeId}/semantic-matches`。
- [ ] 处理无向量数据的情况。
- [ ] 处理向量维度不一致的情况。
- [ ] 处理查询失败情况。

建议 API：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/resumes/{resumeId}/semantic-matches` | 查询简历与岗位描述语义相似片段 |

建议查询参数：

| 参数 | 说明 |
|---|---|
| jobDescriptionId | 岗位描述 ID，必填 |
| topK | 返回相似片段数量，可选 |

建议返回字段：

| 字段 | 说明 |
|---|---|
| resumeId | 简历 ID |
| jobDescriptionId | 岗位描述 ID |
| overallSimilarity | 整体语义相似度，可选 |
| matches | 相似片段列表 |
| resumeChunkText | 简历片段 |
| jobChunkText | 岗位描述片段 |
| similarityScore | 相似度分数 |

验收标准：

- 已生成向量的简历和岗位描述可以进行相似度查询。
- 未生成简历向量时返回明确提示。
- 未生成岗位描述向量时返回明确提示。
- 用户不能查询他人的简历或岗位描述。
- 可以返回 Top-K 相似片段。
- 相似度结果具有基本解释性。
- 本任务不替代 AI 匹配结果。
- 本任务不实现 RAG。

---

## v2.8.7 简单 RAG 检索增强

状态：未开始

目标：

- 基于语义相似度查询结果，为 AI 匹配或优化建议提供检索增强上下文。
- RAG 只作为辅助上下文，不替代原有业务逻辑。
- 先实现最小可用的简单 RAG，不做企业级知识库。

任务：

- [ ] 定义 RAG 检索服务，例如 `ResumeRagService`。
- [ ] 基于简历和岗位描述查询 Top-K 相似片段。
- [ ] 组装 RAG 上下文。
- [ ] 限制上下文长度。
- [ ] 在 AI 匹配 Prompt 中可选加入 RAG 上下文。
- [ ] 在 AI 优化建议 Prompt 中可选加入 RAG 上下文。
- [ ] 明确 RAG 上下文只来源于用户自己的简历和岗位描述。
- [ ] 明确 RAG 不允许引入外部未知内容。
- [ ] 处理无可用相似片段的情况。
- [ ] 记录是否使用 RAG。
- [ ] 记录检索到的片段数量。
- [ ] 在结果中可选返回 RAG 使用说明。

建议 RAG 上下文格式：

| 字段 | 说明 |
|---|---|
| resumeChunk | 简历相关片段 |
| jobChunk | 岗位描述相关片段 |
| similarityScore | 相似度分数 |
| reason | 用于辅助分析的原因 |

建议能力边界：

- 只检索当前用户自己的简历和岗位描述。
- 只取 Top-K 片段作为上下文。
- 不接入外部知识库。
- 不做多轮问答。
- 不做复杂引用溯源系统。
- 不把 RAG 输出作为最终事实，仍需 AI 输出校验。

验收标准：

- 可以基于语义匹配结果组装 RAG 上下文。
- RAG 上下文只来自用户自己的数据。
- RAG 可以辅助 AI 匹配或优化建议。
- 没有可用片段时系统仍可正常运行。
- 不破坏原有 AI 匹配和建议生成流程。
- 不引入外部未知知识。
- 本任务不实现多轮对话。

---

## v2.8.8 v2.8 联调、审查与日志

状态：未开始

目标：

- 完成 v2.8 前后端联调或接口联调。
- 验证 Embedding、pgvector、语义相似度和简单 RAG 闭环。
- 修复向量生成、相似度查询和 RAG 上下文组装中的问题。
- 更新 v2.8 迭代日志。

任务：

- [ ] 测试 pgvector 扩展是否可用。
- [ ] 测试 Embedding 模型调用。
- [ ] 测试简历向量生成。
- [ ] 测试岗位描述向量生成。
- [ ] 测试简历与岗位描述语义相似度查询。
- [ ] 测试 Top-K 相似片段返回。
- [ ] 测试无简历向量情况。
- [ ] 测试无岗位描述向量情况。
- [ ] 测试向量维度不一致情况。
- [ ] 测试用户不能访问他人的向量数据。
- [ ] 测试 RAG 上下文组装。
- [ ] 测试 RAG 对 AI 匹配或优化建议的辅助效果。
- [ ] 检查 Embedding API Key 是否未泄露。
- [ ] 检查日志中是否泄露简历原文、完整 Prompt、API Key 或 Token。
- [ ] 运行必要的后端构建检查。
- [ ] 运行必要的前端构建检查。
- [ ] 更新 `docs/iteration-log/v2.8-vector-rag.md`。
- [ ] 更新 `docs/tasks/phase-3-task-list.md`。

验收标准：

- v2.8 Embedding / pgvector / RAG 初步增强闭环可演示。
- 后端可以正常启动。
- pgvector 可用。
- 简历可以生成向量。
- 岗位描述可以生成向量。
- 可以查询基础语义相似度。
- 可以返回相似片段。
- 简单 RAG 上下文可以组装。
- 语义匹配结果不破坏原有 AI 匹配流程。
- 用户数据隔离正确。
- API Key 和敏感数据未泄露。
- v2.8 迭代日志已更新。

---

## v2.8 禁止提前实现内容

在 v2.8 中不允许提前实现：

- 复杂推荐系统。
- 企业级知识库。
- 大规模向量数据管理平台。
- 多轮对话。
- 职业顾问聊天。
- 全自动职位投递。
- 模型微调。
- 企业级 Prompt 实验平台。
- 复杂 A/B 测试平台。
- 自动化评测平台。
- 微服务拆分。
- Kubernetes。
- 生产级监控系统。

---

## v2.8 完成后的下一步

v2.8 完成后，Phase 3 AI 能力增强阶段结束。

下一步可以选择：

1. 进入 Phase 4 - 微服务与企业部署版。
2. 对 Phase 3 的 AI 能力进行体验优化。
3. 整理项目简历描述和面试讲解材料。
4. 根据岗位方向选择重点包装：Java 后端、AI 应用开发或全栈项目。

Phase 4 再开始处理：

- Spring Cloud Gateway。
- 服务拆分。
- Nacos。
- OpenFeign。
- RabbitMQ。
- Nginx。
- 更完整的 CI/CD。
- Kubernetes。
- Prometheus。
- Grafana。
