# Phase 3 Task List - AI 能力增强阶段

## 0. 文件用途

本文件用于拆解 Phase 3 的具体开发任务。

`docs/phase-3-ai.md` 负责说明第三阶段的总体目标、功能范围、技术范围和阶段边界。  
本文件负责说明 Phase 3 每个版本下面具体怎么拆小任务。

使用原则：

1. 每次只做一个小任务。
2. 当前正在开发的 v2.x 版本详细展开。
3. 已完成版本压缩成总结。
4. 未开始版本可以先保留大纲，进入对应版本前再继续细分。
5. 完成一个 v2.x 版本后，更新对应迭代日志。
6. 不允许跳过当前任务直接实现后续版本功能。
7. AI 输出必须遵守事实一致性原则，不得编造用户不存在的经历、技能、证书、奖项或量化指标。
8. 所有 AI 输出应尽量结构化，关键结果必须保存模型名称、Prompt 版本和生成时间。

---

## 1. Phase 3 总版本划分

| 版本 | 主题 | 状态 |
|---|---|---|
| v2.1 | AI 能力基线检查与提示词规范 | 已完成 |
| v2.2 | 岗位描述输入与结构化解析 | 已完成 |
| v2.3 | 简历与岗位 AI 匹配分析 | 已完成 |
| v2.4 | AI 简历优化建议增强 | 当前版本 |
| v2.5 | AI 局部改写建议 | 未开始 |
| v2.6 | AI 结果持久化与历史回看 | 未开始 |
| v2.7 | AI 输出评估流程 | 未开始 |
| v2.8 | Embedding / pgvector / RAG 初步增强 | 未开始 |

---

## 2. Phase 3 总目标

在 Phase 1 MVP 和 Phase 2 工程化基础上，增强系统的 AI 能力，使系统能够围绕“简历内容 + 岗位描述”完成更深入的分析、匹配、优化和局部改写。

Phase 3 完成后应满足：

- 用户可以提交岗位描述。
- 系统可以解析岗位描述并抽取结构化岗位要求。
- 系统可以基于简历和岗位描述生成结构化匹配反馈。
- 系统可以生成具体、有依据的简历优化建议。
- 系统可以给出局部改写建议。
- AI 输出不会编造用户不存在的经历。
- AI 输出可以持久化和回看。
- AI 输出记录模型名称和 Prompt 版本。
- 系统具备基础 AI 输出评估流程。
- 可选完成基础 Embedding / pgvector / RAG 增强。

---

## 3. Phase 3 禁止提前实现内容

在 Phase 3 中不允许提前实现：

- 全自动职位投递。
- 模型微调。
- 企业级 Prompt 实验平台。
- 大规模招聘网站爬取。
- 自动伪造简历经历。
- 自动生成不存在的证书、奖项、量化指标。
- 复杂 A/B 测试平台。
- 多租户企业后台。
- 微服务拆分。
- RabbitMQ / Nacos / Gateway。
- Kubernetes。
- 复杂监控平台。
- 生产级高可用部署。

---

# v2.1 - AI 能力基线检查与提示词规范

状态：已完成

## v2.1 总结

v2.1 已完成 AI 能力基线检查与提示词规范收口：

- 已确认当前 AI 调用入口位于 `infra/ai/`，核心为 `AiClientService`、`AiClientProperties` 和 `OpenAiCompatibleAiClientService`。
- 已确认 AI 配置通过 `application.yaml` 读取环境变量，模型名称由 `OPENAI_MODEL` 注入，不在业务代码中硬编码。
- 已确认当前唯一业务 AI 能力是“简历 AI 分析”，由 `ResumeAnalysisServiceImpl` 编排。
- 已确认 Prompt 构建和输出解析已拆分到独立 Service，Prompt 版本为 `resume_analysis_v1`。
- 已明确 `infra/ai/`、Prompt 构建、输出解析、业务 Service、Entity / Mapper 和 VO 的分层职责。
- 已更新 `docs/project-structure.md`，补充 AI 分层和后续扩展放置规则。
- 已整理 Prompt 命名、版本、输入字段、输出字段、JSON 约束和安全约束规范。
- 已整理 AI 输出解析流程，明确非 JSON、字段缺失、评分越界、多余字段和脏数据入库策略。
- 已在现有简历分析 Prompt 中补充不得代填量化指标、证书、奖项或项目结果的约束。
- 已在 AI 输出解析中限制单条列表文本长度，避免超长自由文本直接进入结构化结果。
- 已在简历 AI 分析展示和历史 AI 分析摘要中增加“AI 建议需用户确认”的提示。
- 已在 README 中补充 AI 输出限制说明。
- 已确认当前日志不完整打印简历原文，只记录必要状态、模型和脱敏错误原因。
- 已完成 v2.1 相关单元测试、前端构建和文档静态核对。
- 迭代日志已更新：`docs/iteration-log/v2.1-ai-baseline.md`。

说明：

- v2.1 不新增岗位描述解析、AI 匹配、优化建议增强或局部改写。
- v2.1 不引入复杂 Prompt 管理后台、Prompt A/B 测试平台、RAG、pgvector 或微服务拆分。
- 当前 Prompt 和解析服务仍保留在 `module/analysis` 内；后续出现跨业务重复逻辑时再小步抽象。
- 当前只保存结构化 AI 结果和错误信息，不保存原始 AI 输出。

## v2.1 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.1.1 现有 AI 能力检查 | 已完成 |
| v2.1.2 AI 分层结构整理 | 已完成 |
| v2.1.3 Prompt 模板规范 | 已完成 |
| v2.1.4 AI 输出结构与解析规范 | 已完成 |
| v2.1.5 AI 安全规则落地 | 已完成 |
| v2.1.6 联调、审查与日志 | 已完成 |

---

# v2.2 - 岗位描述输入与结构化解析

状态：已完成

## v2.2 总结

v2.2 已完成岗位描述输入与结构化解析闭环：

- 已新增 `job_descriptions` 表迁移脚本 `V8__create_job_descriptions_table.sql`。
- 已新增 `JobDescription` 实体和 `JobDescriptionMapper`。
- 已实现 `POST /api/job-descriptions` 岗位描述提交接口。
- 已实现 `GET /api/job-descriptions` 当前用户岗位描述列表接口。
- 已实现 `GET /api/job-descriptions/{id}` 岗位描述详情接口。
- 已将 `/api/job-descriptions/**` 加入认证保护。
- 已校验标题不能为空、岗位描述原文不能为空，且限制标题和原文最大长度。
- 已保证用户只能查看和解析自己的岗位描述。
- 已创建 `job_description_parse_v1` Prompt，输入为岗位描述原文。
- 已明确 AI 输出字段：`jobTitle`、`requiredSkills`、`bonusSkills`、`experienceSignals`、`responsibilities`、`keywords`、`summary`。
- 已要求 Prompt 不编造岗位描述中不存在的职位、技能、经验、职责或招聘条件。
- 已新增岗位描述结构化解析 Service 和输出 Parser。
- 已实现 AI 调用、JSON 解析、结构化结果保存、模型名称保存和 Prompt 版本保存。
- 已实现 `POST /api/job-descriptions/{id}/parse` 岗位描述解析接口。
- 已处理 AI 调用失败和 AI 返回格式错误，失败时保存 `FAILED` 状态和错误信息，并清空结构化结果。
- 已新增前端岗位描述列表页、输入页和详情页。
- 已接入 `/job-descriptions`、`/job-descriptions/new` 和 `/job-descriptions/:id` 路由，并使用登录守卫保护。
- 已支持前端查看已提交岗位描述、提交岗位描述、触发解析、查看解析状态和结构化解析结果。
- 已展示职位名称、岗位摘要、必备技能、加分技能、经验信号、职责内容和关键词。
- 已修复岗位描述详情页首次进入不加载数据的问题。
- 已将岗位描述解析前端请求超时时间调整为 120 秒。
- 已完成后端相关测试、前端构建和手动联调，用户确认可成功解析。
- 迭代日志已更新：`docs/iteration-log/v2.2-job-description-parse.md`。

说明：

- v2.2 不做简历与岗位 AI 匹配。
- v2.2 不做岗位描述删除功能或复杂岗位管理后台。
- 当前岗位描述解析结果以 JSON 字符串保存在 `structured_content`，后续 v2.3 可直接读取。
- 当前失败状态保存错误信息，不保存原始 AI 输出。

## v2.2 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.2.1 岗位描述表设计 | 已完成 |
| v2.2.2 岗位描述提交接口 | 已完成 |
| v2.2.3 岗位描述解析 Prompt | 已完成 |
| v2.2.4 岗位描述结构化解析服务 | 已完成 |
| v2.2.5 岗位描述解析接口 | 已完成 |
| v2.2.6 前端岗位描述输入与解析展示 | 已完成 |
| v2.2.7 v2.2 联调、审查与日志 | 已完成 |

---

# v2.3 - 简历与岗位 AI 匹配分析

状态：已完成

## v2.3 总结

v2.3 已完成简历与岗位描述的 AI 匹配分析闭环：

- 已新增 `ai_job_match_results` 表迁移脚本 `V9__create_ai_job_match_results_table.sql`。
- 已新增 `AiJobMatchResult` 实体和 `AiJobMatchResultMapper`。
- 已建立 `resume_id`、`job_description_id` 与匹配结果的关联关系，并对同一简历和同一岗位描述设置唯一约束。
- 已明确 AI 匹配结果字段：总体分数、强匹配项、弱匹配项、缺失技能、表达较弱经历、匹配依据、风险提示、模型名称、Prompt 版本和错误信息。
- 已创建 `ai_job_match_v1` Prompt，输入为简历结构化解析结果和岗位描述结构化解析结果。
- 已要求 Prompt 只基于输入内容分析，不编造用户不存在的经历、技能、项目、证书、奖项或量化指标。
- 已新增 AI 匹配 Prompt 构建、输出解析和业务编排服务。
- 已实现 AI 输出 JSON 解析、分数截断、字段兜底和失败状态保存。
- 已实现 `POST /api/resumes/{resumeId}/ai-job-matches`，用于触发当前用户简历与岗位描述的 AI 匹配。
- 已实现 `GET /api/resumes/{resumeId}/ai-job-matches`，支持查询简历下的匹配结果列表，也支持通过 `jobDescriptionId` 查询指定结果。
- 已保证用户只能匹配和查看自己的简历、岗位描述和 AI 匹配结果。
- 已新增前端 AI 匹配页面 `/ai-job-matches`，并通过登录守卫保护。
- 已支持前端选择简历和已解析岗位描述，触发 AI 匹配并展示结构化结果。
- 已展示总体分数、强匹配项、弱匹配项、缺失技能、表达较弱经历、匹配依据、风险提示、模型名称、Prompt 版本和更新时间。
- 已接入首页、岗位描述列表页和岗位描述详情页的 AI 匹配入口。
- 已将 AI 匹配请求超时时间设置为 120 秒，并将默认 `OPENAI_MAX_TOKENS` 调整为 4000。
- 已增强 OpenAI-compatible 客户端对空响应、截断响应和非 JSON 响应的错误提示。
- 已优化 AI 匹配 Prompt 输入长度，降低输出超限和非 JSON 结果概率。
- 已在历史记录中接入 AI 岗位匹配结果，最近匹配可以显示并跳转到 AI 匹配页面。
- 已修复简历删除时未清理 AI 匹配结果导致的删除失败问题。
- 已新增岗位描述删除接口和前端删除入口，删除岗位描述时同步清理关联 AI 匹配结果。
- 已完成后端相关测试、前端构建和本地联调，用户确认 AI 匹配可以成功解析并展示。
- 迭代日志已更新：`docs/iteration-log/v2.3-ai-match.md`。

说明：

- v2.3 不生成最终简历优化方案。
- v2.3 不生成局部改写内容。
- v2.3 不做向量语义匹配、RAG、pgvector 或多轮对话。
- 当前 AI 匹配结果以结构化 JSON 字符串保存，后续 v2.4 可基于成功匹配结果生成优化建议。
- 当前失败状态保存错误信息，不保存原始 AI 输出。

## v2.3 小任务完成情况

| 小任务 | 状态 |
|---|---|
| v2.3.1 AI 匹配结果表设计 | 已完成 |
| v2.3.2 AI 匹配 Prompt 设计 | 已完成 |
| v2.3.3 AI 匹配分析服务 | 已完成 |
| v2.3.4 AI 匹配触发接口 | 已完成 |
| v2.3.5 AI 匹配结果查询接口 | 已完成 |
| v2.3.6 前端 AI 匹配结果展示 | 已完成 |
| v2.3.7 v2.3 联调、审查与日志 | 已完成 |

---

# v2.4 - AI 简历优化建议增强

状态：当前版本

## v2.4 目标

基于 AI 匹配分析结果，生成更具体、更有依据的简历优化建议。

v2.4 只负责“基于 AI 匹配结果生成优化建议”，不负责局部改写完整句子，不负责生成最终简历版本，不负责 RAG、pgvector 和多轮对话。

v2.4 完成后应满足：

- 系统可以基于 v2.3 的 AI 匹配结果生成优化建议。
- 优化建议必须具体、可审阅、可执行。
- 优化建议必须有依据，能够关联到简历内容或岗位要求。
- 系统可以区分技能缺口、经历表达不足、项目描述弱、优势突出等不同建议类型。
- 系统可以为建议设置优先级。
- AI 不得编造用户不存在的经历、技能、证书、奖项或量化指标。
- 用户可以查看自己的优化建议结果。
- 前端可以展示建议类型、优先级、问题、建议内容和依据。
- 本轮结果记录到 `docs/iteration-log/v2.4-ai-suggestions.md`。

说明：

- v2.4 不做局部改写结果对比，局部改写放到 v2.5。
- v2.4 不生成完整定制版简历。
- v2.4 不做 RAG。
- v2.4 不做 pgvector。
- v2.4 不做多轮对话。
- v2.4 的重点是让 AI 建议“具体、有依据、可保存、可展示”。

---

## v2.4.1 优化建议结果结构设计

状态：当前任务

目标：

- 设计 AI 简历优化建议结果表。
- 明确优化建议与 AI 匹配结果、简历、岗位描述之间的关系。
- 明确建议类型、优先级、目标简历部分、问题描述、建议内容和依据字段。
- 创建优化建议实体和 Mapper。

任务：

- [ ] 设计 `ai_resume_suggestions` 表字段。
- [ ] 明确 `resume_id` 与 `resumes.id` 的关联关系。
- [ ] 明确 `job_description_id` 与 `job_descriptions.id` 的关联关系。
- [ ] 明确 `ai_job_match_result_id` 与 `ai_job_match_results.id` 的关联关系。
- [ ] 明确建议状态字段 `suggestion_status`。
- [ ] 明确建议结果字段 `suggestions`。
- [ ] 明确模型名称字段 `model_name`。
- [ ] 明确 Prompt 版本字段 `prompt_version`。
- [ ] 明确错误信息字段 `error_message`。
- [ ] 明确创建时间字段 `created_at`。
- [ ] 明确更新时间字段 `updated_at`。
- [ ] 创建数据库迁移脚本。
- [ ] 创建 `module/analysis/entity/AiResumeSuggestion.java`。
- [ ] 创建 `module/analysis/mapper/AiResumeSuggestionMapper.java`。

建议字段：

| 字段名 | 说明 |
|---|---|
| id | 主键 |
| resume_id | 关联简历 ID |
| job_description_id | 关联岗位描述 ID |
| ai_job_match_result_id | 关联 AI 匹配结果 ID |
| suggestion_status | 建议生成状态 |
| suggestions | 优化建议列表，建议 JSON 字符串 |
| model_name | 模型名称 |
| prompt_version | Prompt 版本 |
| error_message | 错误信息 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

建议状态：

| 状态 | 说明 |
|---|---|
| PENDING | 待生成 |
| SUCCESS | 生成成功 |
| FAILED | 生成失败 |

建议单条建议结构：

| 字段 | 说明 |
|---|---|
| type | 建议类型 |
| priority | 优先级 |
| targetSection | 作用于简历哪个部分 |
| issue | 当前问题 |
| suggestion | 修改建议 |
| evidence | 建议依据 |
| caution | 注意事项 |
| relatedItems | 关联技能、经历或岗位要求 |

建议类型：

| 类型 | 说明 |
|---|---|
| SKILL_GAP | 技能缺口 |
| EXPERIENCE_WEAKNESS | 经历表达不足 |
| PROJECT_DESCRIPTION | 项目描述优化 |
| HIGHLIGHT_STRENGTH | 优势突出 |
| STRUCTURE | 简历结构优化 |
| GENERAL | 综合建议 |

建议优先级：

| 优先级 | 说明 |
|---|---|
| HIGH | 高优先级 |
| MEDIUM | 中优先级 |
| LOW | 低优先级 |

验收标准：

- `ai_resume_suggestions` 表结构明确。
- `resume_id`、`job_description_id`、`ai_job_match_result_id` 关系明确。
- AiResumeSuggestion 实体与表字段对应。
- AiResumeSuggestionMapper 可以被 MyBatis-Plus 扫描。
- 建议 JSON 结构明确。
- 不实现优化建议 Prompt。
- 不实现优化建议生成服务。
- 不实现优化建议接口。
- 不实现前端页面。

---

## v2.4.2 优化建议 Prompt 设计

状态：未开始

目标：

- 设计基于 AI 匹配结果生成优化建议的 Prompt。
- 要求 AI 只基于简历内容、岗位描述和 AI 匹配结果提出建议。
- 输出必须是结构化 JSON。
- 明确禁止编造经历、技能、证书、奖项和量化指标。

任务：

- [ ] 创建 Prompt 版本 `resume_suggestion_v1`。
- [ ] 明确 Prompt 输入：简历结构化解析结果。
- [ ] 明确 Prompt 输入：岗位描述结构化解析结果。
- [ ] 明确 Prompt 输入：AI 匹配结果。
- [ ] 明确 Prompt 输出 JSON 结构。
- [ ] 要求 AI 根据缺失技能生成建议。
- [ ] 要求 AI 根据弱匹配项生成建议。
- [ ] 要求 AI 根据表达较弱经历生成建议。
- [ ] 要求 AI 根据强匹配项生成优势突出建议。
- [ ] 要求 AI 为每条建议提供依据。
- [ ] 要求 AI 区分“可直接修改表达”和“需要用户补充真实内容”。
- [ ] 要求 AI 不得编造简历中不存在的经历。
- [ ] 要求 AI 不直接生成完整简历。
- [ ] 处理简历内容不足的情况。
- [ ] 处理匹配结果不足的情况。

建议 Prompt 输入：

| 输入 | 说明 |
|---|---|
| resumeStructuredContent | 简历结构化解析结果 |
| jobStructuredContent | 岗位描述结构化解析结果 |
| aiMatchResult | AI 匹配结果 |
| promptVersion | Prompt 版本 |

建议 AI 输出结构：

```json
{
  "suggestions": [
    {
      "type": "SKILL_GAP",
      "priority": "HIGH",
      "targetSection": "技能",
      "issue": "岗位要求 Docker，但简历中未体现 Docker 相关内容",
      "suggestion": "如果你确实掌握 Docker，建议在技能部分补充 Docker，并在项目经历中说明容器化部署实践；如果暂未掌握，应先补充学习和真实实践。",
      "evidence": [
        "岗位要求中包含 Docker",
        "简历解析结果中未发现 Docker"
      ],
      "caution": "不要在未真实掌握 Docker 的情况下直接写入简历。",
      "relatedItems": ["Docker"]
    }
  ]
}
```

输出字段说明：

| 字段 | 说明 |
|---|---|
| type | 建议类型 |
| priority | 优先级 |
| targetSection | 目标简历部分 |
| issue | 当前问题 |
| suggestion | 优化建议 |
| evidence | 建议依据 |
| caution | 注意事项 |
| relatedItems | 关联项 |

验收标准：

- Prompt 版本明确。
- Prompt 输入明确。
- Prompt 输出 JSON 结构明确。
- Prompt 明确要求建议有依据。
- Prompt 明确要求不编造经历。
- Prompt 能区分表达优化和需要补充真实内容。
- Prompt 不直接生成完整简历。
- 本任务不实现服务代码。
- 本任务不实现接口。
- 本任务不实现前端页面。

---

## v2.4.3 优化建议生成服务

状态：未开始

目标：

- 基于简历解析结果、岗位描述解析结果和 AI 匹配结果调用 AI。
- 生成结构化优化建议。
- 解析并校验 AI 返回结果。
- 保存优化建议。

任务：

- [ ] 创建优化建议生成服务，例如 `AiResumeSuggestionService`。
- [ ] 查询简历元数据。
- [ ] 校验当前用户是否拥有该简历。
- [ ] 查询岗位描述记录。
- [ ] 校验当前用户是否拥有该岗位描述。
- [ ] 查询 AI 匹配结果。
- [ ] 校验 AI 匹配结果是否成功。
- [ ] 查询简历结构化解析结果。
- [ ] 查询岗位描述结构化解析结果。
- [ ] 组装 `resume_suggestion_v1` Prompt。
- [ ] 调用 AI 客户端服务。
- [ ] 解析 AI 返回 JSON。
- [ ] 校验建议列表字段。
- [ ] 校验建议类型是否合法。
- [ ] 校验优先级是否合法。
- [ ] 校验每条建议是否有依据。
- [ ] 保存优化建议结果。
- [ ] 保存模型名称和 Prompt 版本。
- [ ] 处理 AI 调用失败。
- [ ] 处理 AI 返回格式异常。
- [ ] 明确重复生成策略。

重复生成策略：

- Phase 3 默认覆盖同一 AI 匹配结果对应的旧优化建议。
- 如果同一 `ai_job_match_result_id` 已经存在建议结果，再次生成时更新原记录。
- 如果 AI 建议生成失败，应记录失败状态和错误信息。
- AI 返回格式错误时不得保存成功状态结果。

验收标准：

- 成功的 AI 匹配结果可以生成优化建议。
- 未完成 AI 匹配不能直接生成优化建议。
- 用户不能为他人的简历生成建议。
- 用户不能为他人的岗位描述生成建议。
- 优化建议可以保存。
- 每条建议有类型、优先级、问题、建议和依据。
- AI 调用失败时记录失败状态和错误信息。
- AI 返回格式错误时不保存脏数据。
- 重复生成会覆盖旧结果。
- 本任务不实现前端页面。
- 本任务不生成局部改写文本。
- 本任务不做 RAG 或向量检索。

---

## v2.4.4 优化建议触发接口

状态：未开始

目标：

- 实现触发 AI 简历优化建议生成的接口。
- 保证用户只能基于自己的简历、岗位描述和 AI 匹配结果生成建议。
- 返回建议结果摘要。

任务：

- [ ] 实现 `POST /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 创建优化建议请求 DTO。
- [ ] 请求体中接收 `jobDescriptionId`。
- [ ] 请求体中可选接收 `aiJobMatchResultId`。
- [ ] 根据当前登录用户校验简历归属。
- [ ] 根据当前登录用户校验岗位描述归属。
- [ ] 校验 AI 匹配结果是否存在且成功。
- [ ] 调用优化建议生成服务。
- [ ] 返回建议结果摘要。
- [ ] 处理未完成 AI 匹配的情况。
- [ ] 处理 AI 调用失败情况。
- [ ] 保证接口返回结构统一。

建议 API：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/resumes/{resumeId}/ai-suggestions` | 触发 AI 优化建议生成 |

建议请求体：

```json
{
  "jobDescriptionId": 1,
  "aiJobMatchResultId": 1
}
```

建议返回字段：

| 字段 | 说明 |
|---|---|
| suggestionId | 优化建议结果 ID |
| resumeId | 简历 ID |
| jobDescriptionId | 岗位描述 ID |
| aiJobMatchResultId | AI 匹配结果 ID |
| suggestionStatus | 建议状态 |
| suggestionCount | 建议数量 |
| updatedAt | 更新时间 |

验收标准：

- 已登录用户可以为自己的简历生成优化建议。
- 用户不能为他人的简历生成建议。
- 用户不能为他人的岗位描述生成建议。
- 未完成 AI 匹配时返回明确提示。
- 生成成功后返回建议结果摘要。
- 生成失败时返回明确错误信息。
- 本接口不返回 API Key、完整 Prompt 等敏感信息。
- 本接口不生成完整简历版本。

---

## v2.4.5 优化建议查询接口

状态：未开始

目标：

- 实现 AI 优化建议结果查询接口。
- 保证用户只能查看自己的优化建议。
- 返回结构化建议列表。

任务：

- [ ] 实现 `GET /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 支持通过 `jobDescriptionId` 查询指定岗位描述下的建议。
- [ ] 支持通过 `aiJobMatchResultId` 查询指定匹配结果对应的建议。
- [ ] 根据当前登录用户校验简历归属。
- [ ] 根据当前登录用户校验岗位描述归属。
- [ ] 查询优化建议结果。
- [ ] 创建优化建议结果 VO。
- [ ] 返回建议状态。
- [ ] 返回建议列表。
- [ ] 返回建议类型。
- [ ] 返回建议优先级。
- [ ] 返回目标简历部分。
- [ ] 返回当前问题。
- [ ] 返回优化建议。
- [ ] 返回建议依据。
- [ ] 返回注意事项。
- [ ] 返回模型名称和 Prompt 版本。
- [ ] 处理无建议结果的情况。
- [ ] 处理建议生成失败状态。

建议 API：

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/resumes/{resumeId}/ai-suggestions` | 查询 AI 优化建议 |

建议查询参数：

| 参数 | 说明 |
|---|---|
| jobDescriptionId | 岗位描述 ID，可选 |
| aiJobMatchResultId | AI 匹配结果 ID，可选 |

建议返回字段：

| 字段 | 说明 |
|---|---|
| suggestionId | 优化建议结果 ID |
| resumeId | 简历 ID |
| jobDescriptionId | 岗位描述 ID |
| aiJobMatchResultId | AI 匹配结果 ID |
| suggestionStatus | 建议状态 |
| suggestions | 优化建议列表 |
| modelName | 模型名称 |
| promptVersion | Prompt 版本 |
| errorMessage | 错误信息 |
| updatedAt | 更新时间 |

验收标准：

- 用户可以查看自己的优化建议。
- 用户不能查看他人的优化建议。
- 无建议结果时返回明确提示或空列表。
- 建议生成失败时返回失败状态和错误信息。
- 返回结构适合前端展示。
- 返回结果不包含 API Key 和完整 Prompt。
- 本任务不实现前端页面。

---

## v2.4.6 前端优化建议展示

状态：未开始

目标：

- 前端提供优化建议生成入口。
- 前端展示结构化优化建议。
- 用户可以根据建议类型、优先级和目标简历部分理解修改方向。

任务：

- [ ] 在 AI 匹配结果页面增加“生成优化建议”按钮。
- [ ] 创建优化建议 API 文件。
- [ ] 调用 `POST /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 调用 `GET /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 展示建议状态。
- [ ] 展示建议数量。
- [ ] 展示建议类型。
- [ ] 展示建议优先级。
- [ ] 展示目标简历部分。
- [ ] 展示当前问题。
- [ ] 展示优化建议。
- [ ] 展示建议依据。
- [ ] 展示注意事项。
- [ ] 展示模型名称和分析时间。
- [ ] 处理未完成 AI 匹配提示。
- [ ] 处理建议生成失败提示。
- [ ] 处理加载状态。
- [ ] 增加“AI 建议需用户确认，不应直接伪造经历”的提示。

页面展示建议：

- 建议生成操作区域。
- 高优先级建议区域。
- 技能缺口建议区域。
- 经历表达建议区域。
- 优势突出建议区域。
- 综合建议区域。
- 建议依据展示区域。
- 注意事项展示区域。
- 错误信息展示区域。

验收标准：

- 用户可以在前端触发优化建议生成。
- 生成完成后可以看到建议列表。
- 前端可以区分建议类型和优先级。
- 每条建议可以看到问题、建议和依据。
- 未完成 AI 匹配时不能直接生成建议，前端有明确提示。
- 生成失败时有明确错误提示。
- 未登录用户不能访问优化建议页面。
- 用户不能查看他人的优化建议。
- 前端不展示 API Key、完整 Prompt 等敏感信息。
- 前端有 AI 建议需用户确认的提示。

---

## v2.4.7 v2.4 联调、审查与日志

状态：未开始

目标：

- 完成 v2.4 前后端联调。
- 验证 AI 优化建议生成闭环。
- 修复建议生成、结果保存和前端展示中的问题。
- 更新 v2.4 迭代日志。

任务：

- [ ] 联调 `POST /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 联调 `GET /api/resumes/{resumeId}/ai-suggestions`。
- [ ] 使用成功 AI 匹配结果测试优化建议生成。
- [ ] 测试未完成 AI 匹配时触发建议生成。
- [ ] 测试 AI 调用失败场景。
- [ ] 测试 AI 返回格式错误场景。
- [ ] 测试重复生成覆盖策略。
- [ ] 测试用户只能为自己的简历生成建议。
- [ ] 测试用户只能查看自己的建议结果。
- [ ] 测试前端优化建议展示。
- [ ] 检查建议是否有具体依据。
- [ ] 检查建议是否编造不存在的经历。
- [ ] 检查日志中是否泄露简历原文、Prompt、API Key 或 Token。
- [ ] 运行必要的后端构建检查。
- [ ] 运行必要的前端构建检查。
- [ ] 更新 `docs/iteration-log/v2.4-ai-suggestions.md`。
- [ ] 更新 `docs/tasks/phase-3-task-list.md`。

验收标准：

- v2.4 AI 简历优化建议闭环可演示。
- 后端和前端都可以正常启动。
- 成功 AI 匹配结果可以生成优化建议。
- 优化建议可以保存和查询。
- 优化建议结构化、具体、有依据。
- AI 不编造用户不存在的经历。
- 用户数据隔离正确。
- AI 调用失败时系统不会崩溃。
- AI 返回格式异常时不保存脏数据。
- 日志不泄露敏感信息。
- v2.4 迭代日志已更新。

---

## v2.4 禁止提前实现内容

在 v2.4 中不允许提前实现：

- AI 局部改写建议。
- AI 历史结果统一管理。
- AI 输出评估流程。
- Embedding。
- pgvector。
- RAG。
- 多轮对话。
- 职业顾问聊天。
- 全自动职位投递。
- 模型微调。
- 企业级 Prompt 实验平台。
- 完整定制版简历生成。
- 微服务拆分。

---

## v2.4 完成后的下一步

v2.4 完成后，进入：

`v2.5 - AI 局部改写建议`

v2.5 再开始处理：

- 局部改写结果表设计。
- 局部改写 Prompt 设计。
- 局部改写服务。
- 局部改写接口。
- 前端原文与改写建议对比展示。
- 用户确认状态记录。

# v2.5 - AI 局部改写建议

状态：未开始

## v2.5 目标

支持对简历中的项目经历、技能描述、实习经历等局部内容进行表达优化，但不生成虚假经历。

---

## v2.5 建议小任务

- v2.5.1 局部改写结果表设计
- v2.5.2 局部改写 Prompt 设计
- v2.5.3 局部改写服务
- v2.5.4 局部改写接口
- v2.5.5 前端原文与改写建议对比展示
- v2.5.6 用户确认状态记录
- v2.5.7 v2.5 联调、审查与日志

进入 v2.5 前再详细展开。

---

# v2.6 - AI 结果持久化与历史回看

状态：未开始

## v2.6 目标

统一管理 AI 分析、岗位描述解析、AI 匹配、优化建议和局部改写结果，便于用户回看和后续迭代。

---

## v2.6 建议小任务

- v2.6.1 AI 结果元数据统一
- v2.6.2 AI 历史记录聚合查询设计
- v2.6.3 AI 结果列表接口
- v2.6.4 AI 结果详情接口
- v2.6.5 前端 AI 历史页面
- v2.6.6 v2.6 联调、审查与日志

进入 v2.6 前再详细展开。

---

# v2.7 - AI 输出评估流程

状态：未开始

## v2.7 目标

建立基础 AI 输出质量评估流程，避免 Prompt 修改后质量不可控。

---

## v2.7 建议小任务

- v2.7.1 评估集目录设计
- v2.7.2 样例简历准备
- v2.7.3 样例岗位描述准备
- v2.7.4 评估维度与评分表设计
- v2.7.5 Prompt 输出结果记录
- v2.7.6 评估问题汇总与 Prompt 修改规则
- v2.7.7 v2.7 日志与总结

进入 v2.7 前再详细展开。

---

# v2.8 - Embedding / pgvector / RAG 初步增强

状态：未开始

## v2.8 目标

在 AI 输出结构和评估流程稳定后，尝试引入基础语义匹配或检索增强能力，为后续更高级 AI 能力打基础。

---

## v2.8 建议小任务

- v2.8.1 pgvector 可行性检查
- v2.8.2 向量表设计
- v2.8.3 Embedding 模型接入
- v2.8.4 简历向量生成
- v2.8.5 岗位描述向量生成
- v2.8.6 基础语义相似度查询
- v2.8.7 简单 RAG 检索增强
- v2.8.8 v2.8 联调、审查与日志

进入 v2.8 前再详细展开。

---

## Phase 3 完成后的下一步

Phase 3 完成后，可以进入：

`Phase 4 - 微服务与企业部署版`

Phase 4 再开始处理：

- Spring Cloud Gateway。
- 服务拆分。
- Nacos。
- OpenFeign。
- RabbitMQ。
- Nginx。
- 更完整的 CI/CD。
- Kubernetes。
- Prometheus。
- Grafana。
