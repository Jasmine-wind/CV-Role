# Phase 4 Task List - 架构演进阶段

## 0. 文件用途

本文件用于拆解 Phase 4 的具体开发任务。

`docs/phase-4-architecture.md` 负责说明第四阶段的总体目标、范围、架构方向和退出标准。  
本文件负责说明 Phase 4 每个版本下具体怎么拆小任务、怎么验收、哪些内容不能提前做。

使用原则：

1. 每次只做一个小任务。
2. 当前正在开发的 v3.x 版本详细展开。
3. 已完成版本压缩成总结。
4. 未开始版本先保留任务大纲，进入对应版本前再继续细分。
5. 完成一个 v3.x 版本后，更新对应迭代日志。
6. 不允许跳过当前任务直接实现后续版本功能。
7. Phase 4 的重点是架构演进、稳定性、异步化、安全和部署准备，不是继续堆 AI 功能。
8. Phase 4 不做微服务拆分，除非后续已经有明确瓶颈和必要性。

---

## 1. Phase 4 总版本划分

| 版本 | 主题 | 状态 |
|---|---|---|
| v3.1 | 架构现状审查与领域边界收敛 | 当前版本 |
| v3.2 | 面向领域的包结构整理 | 未开始 |
| v3.3 | 文件存储抽象与存储策略演进 | 未开始 |
| v3.4 | 长耗时任务异步化设计 | 未开始 |
| v3.5 | 解析与 AI 任务状态机落地 | 未开始 |
| v3.6 | 安全加固与敏感信息治理 | 未开始 |
| v3.7 | 可部署配置与环境隔离 | 未开始 |
| v3.8 | 运维文档、架构复盘与阶段收口 | 未开始 |


---

## 2. Phase 4 总目标

将项目从“功能可用的应用”演进为“结构清晰、边界稳定、可维护、可部署、可排查”的系统。

Phase 4 完成后应满足：

- 核心模块具备稳定边界。
- 业务代码按领域职责组织，而不是随功能堆叠。
- 文件存储通过接口访问，便于本地存储和对象存储切换。
- 长耗时解析和 AI 任务具备明确执行模型。
- 解析、AI 分析、匹配、优化、改写、向量生成等任务具备状态追踪能力。
- 安全假设已经记录，并在代码中落实。
- 本地环境、开发环境和部署环境配置边界清晰。
- 敏感配置不硬编码、不提交。
- 运维和部署文档清晰。
- 不引入没有明确必要性的微服务和分布式复杂度。

---

## 3. Phase 4 禁止提前实现内容

在 Phase 4 中不允许提前实现：

- Spring Cloud 微服务拆分。
- Nacos 服务注册与发现。
- OpenFeign 服务间调用。
- Spring Cloud Gateway。
- Kubernetes。
- 复杂服务治理。
- 复杂多租户 SaaS。
- 商业化支付系统。
- 没有瓶颈依据的消息队列强行改造。
- 全量重写已有稳定功能。
- 为了“架构高级”而牺牲项目稳定性。

说明：

- Phase 4 可以为未来微服务预留边界。
- Phase 4 不直接拆微服务。
- 如果需要引入 RabbitMQ，也必须先证明同步任务已经影响体验或可靠性。
- 默认优先使用单体内异步任务，而不是上来就引入消息队列。

---

# v3.1 - 架构现状审查与领域边界收敛

状态：当前版本

## v3.1 目标

在正式进行架构演进前，先审查当前项目的模块结构、包结构、领域边界、任务执行方式、存储方式、安全假设和部署配置，形成清晰的架构问题清单与后续调整优先级。

v3.1 不做大规模代码重构，不改数据库结构，不新增业务功能，主要完成架构现状审查和演进方案确认。

---

## v3.1.1 当前后端模块结构审查

状态：当前任务

目标：

- 梳理当前后端包结构。
- 明确当前代码是否按领域模块组织。
- 找出职责混乱、包边界不清、公共逻辑散落的问题。
- 为 v3.2 面向领域的包结构整理做准备。

任务：

- [ ] 梳理 `backend/src/main/java/com/winter/airesumeoptimizer/` 当前目录结构。
- [ ] 梳理 `common` 目录职责。
- [ ] 梳理 `config` 目录职责。
- [ ] 梳理 `security` 目录职责。
- [ ] 梳理 `infra` 目录职责。
- [ ] 梳理 `module` 目录下各业务模块。
- [ ] 检查用户、简历、解析、岗位、AI、历史、向量等代码是否边界清晰。
- [ ] 检查是否存在 Controller 直接写复杂业务逻辑。
- [ ] 检查是否存在 Service 职责过重。
- [ ] 检查是否存在重复 DTO / VO。
- [ ] 检查是否存在工具类滥用。
- [ ] 记录需要调整的包和类。
- [ ] 更新 `docs/architecture-review.md`。

建议审查维度：

| 维度 | 检查内容 |
|---|---|
| 包结构 | 是否按领域和基础设施区分 |
| Controller | 是否只负责接收请求和返回结果 |
| Service | 是否承担过多职责 |
| Mapper | 是否只负责数据访问 |
| DTO / VO | 是否命名清晰、复用合理 |
| infra | 是否存放外部服务、存储、AI 客户端、Embedding 客户端等基础设施 |
| common | 是否只存放通用结果、异常、常量、工具等低耦合内容 |

验收标准：

- 当前后端结构已梳理。
- 架构问题清单已形成。
- 需要调整的模块和类已记录。
- 不进行大规模代码移动。
- 不修改业务逻辑。
- 不修改数据库结构。
- 本轮结果记录到 `docs/architecture-review.md`。

---

## v3.1.2 当前前端模块结构审查

状态：未开始

目标：

- 梳理当前前端页面、路由、API、Store 和组件结构。
- 找出页面职责重叠、目录混乱、接口调用散落的问题。
- 为后续产品化优化和组件整理做准备。

任务：

- [ ] 梳理 `web/src/views/` 当前页面结构。
- [ ] 梳理 `web/src/router/` 路由结构。
- [ ] 梳理 `web/src/api/` 接口文件结构。
- [ ] 梳理 `web/src/stores/` Pinia 状态结构。
- [ ] 梳理 `web/src/components/` 组件结构。
- [ ] 检查页面是否按产品模块组织。
- [ ] 检查接口调用是否统一放在 API 层。
- [ ] 检查是否存在页面中直接写重复请求逻辑。
- [ ] 检查普通用户页面和管理员页面是否混合。
- [ ] 记录需要调整的页面和目录。
- [ ] 更新 `docs/architecture-review.md`。

验收标准：

- 当前前端结构已梳理。
- 页面、路由、API、Store 和组件问题已记录。
- 不进行大规模 UI 重写。
- 不修改业务接口。
- 不破坏现有页面功能。

---

## v3.1.3 长耗时任务现状审查

状态：未开始

目标：

- 梳理当前文件解析、AI 分析、岗位解析、匹配分析、优化建议、局部改写、向量生成等任务的执行方式。
- 判断哪些任务适合保持同步，哪些任务后续需要异步化。
- 为 v3.4 和 v3.5 做准备。

任务：

- [ ] 梳理简历解析任务执行方式。
- [ ] 梳理 AI 简历诊断任务执行方式。
- [ ] 梳理目标岗位解析任务执行方式。
- [ ] 梳理匹配分析任务执行方式。
- [ ] 梳理岗位优化建议任务执行方式。
- [ ] 梳理局部改写任务执行方式。
- [ ] 梳理 Embedding 向量生成任务执行方式。
- [ ] 记录每类任务平均耗时，可先估算。
- [ ] 记录每类任务失败场景。
- [ ] 判断哪些任务需要状态字段。
- [ ] 判断哪些任务需要前端轮询。
- [ ] 判断是否需要单体内异步线程池。
- [ ] 判断是否暂不需要 RabbitMQ。
- [ ] 更新 `docs/architecture-review.md`。

建议任务分类：

| 任务 | 当前方式 | 后续建议 |
|---|---|---|
| 简历上传 | 同步 | 保持同步 |
| 文本解析 | 同步或短任务 | 可异步化 |
| AI 简历诊断 | 同步 | 可异步化 |
| 岗位描述解析 | 同步 | 可异步化 |
| 匹配分析 | 同步 | 可异步化 |
| 优化建议 | 同步 | 可异步化 |
| 局部改写 | 同步 | 可保持同步或异步 |
| 向量生成 | 同步 | 建议异步化 |

验收标准：

- 所有长耗时任务已梳理。
- 任务执行方式已记录。
- 需要异步化的任务已初步确认。
- 不引入消息队列。
- 不修改现有任务执行方式。
- 为 v3.4 提供依据。

---

## v3.1.4 存储、安全与部署现状审查

状态：未开始

目标：

- 梳理当前文件存储、敏感配置、安全控制、部署配置情况。
- 找出上线前必须修复的问题。
- 为后续存储抽象、安全加固和部署配置做准备。

任务：

- [ ] 检查当前简历文件存储方式。
- [ ] 检查是否已有 FileStorageService 抽象。
- [ ] 检查本地存储路径是否可配置。
- [ ] 检查是否支持后续切换 MinIO。
- [ ] 检查上传文件大小限制。
- [ ] 检查上传文件类型限制。
- [ ] 检查文件访问是否校验用户权限。
- [ ] 检查 JWT Secret 是否通过环境变量配置。
- [ ] 检查 AI Key 是否通过环境变量配置。
- [ ] 检查 Embedding API Key 是否通过环境变量配置。
- [ ] 检查数据库密码是否通过环境变量配置。
- [ ] 检查日志是否打印敏感信息。
- [ ] 检查 CORS 配置。
- [ ] 检查本地和部署环境配置是否分离。
- [ ] 更新 `docs/architecture-review.md`。

验收标准：

- 存储现状已记录。
- 安全风险已记录。
- 部署配置问题已记录。
- 不修改代码。
- 不修改数据库。
- 后续优先级明确。

---

## v3.1.5 v3.1 架构审查日志

状态：未开始

目标：

- 汇总 v3.1 的架构审查结果。
- 形成 Phase 4 后续版本的调整依据。

任务：

- [ ] 创建或更新 `docs/architecture-review.md`。
- [ ] 更新 `docs/tasks/phase-4-task-list.md`。
- [ ] 创建 `docs/iteration-log/v3.1-architecture-review.md`。
- [ ] 记录当前架构优点。
- [ ] 记录当前架构问题。
- [ ] 记录 Phase 4 调整优先级。
- [ ] 记录不做微服务拆分的原因。
- [ ] 准备进入 v3.2。

验收标准：

- v3.1 架构审查完成。
- 架构问题清单明确。
- 后续调整优先级明确。
- 迭代日志已更新。
- 可以进入 v3.2。

---

# v3.2 - 面向领域的包结构整理

状态：未开始

## v3.2 目标

在不改变核心业务行为的前提下，整理后端代码包结构，使用户、简历、解析、岗位、AI、历史、存储、安全等职责边界更加清晰。

v3.2 是小步代码结构整理版本，不做功能重写，不改接口语义，不改数据库结构。

---

## v3.2 建议小任务

- v3.2.1 目标包结构确认
- v3.2.2 领域模块边界整理
- v3.2.3 Controller / Service 职责整理
- v3.2.4 基础设施层职责整理
- v3.2.5 v3.2 联调、审查与日志

进入 v3.2 前再详细展开。

---

# v3.3 - 文件存储抽象与存储策略演进

状态：未开始

## v3.3 目标

完善文件存储抽象，使简历文件访问不依赖具体本地路径，便于后续从本地存储切换到 MinIO 或其他对象存储。

---

## v3.3 建议小任务

- v3.3.1 当前文件存储实现审查
- v3.3.2 FileStorageService 接口规范
- v3.3.3 本地文件存储实现整理
- v3.3.4 MinIO 预留配置与实现方案
- v3.3.5 文件访问安全校验
- v3.3.6 v3.3 联调、审查与日志

进入 v3.3 前再详细展开。

---

# v3.4 - 长耗时任务异步化设计

状态：未开始

## v3.4 目标

为解析、AI 分析、匹配、优化建议、向量生成等长耗时任务建立异步执行模型，避免用户长时间等待同步接口返回。

---

## v3.4 建议小任务

- v3.4.1 长耗时任务异步化方案设计
- v3.4.2 单体内线程池配置
- v3.4.3 任务状态字段与状态流转设计
- v3.4.4 前端轮询策略设计
- v3.4.5 异步任务错误处理策略
- v3.4.6 v3.4 文档与验证

进入 v3.4 前再详细展开。

---

# v3.5 - 解析与 AI 任务状态机落地

状态：未开始

## v3.5 目标

将前面设计的任务状态模型落地到解析、AI 分析、匹配、优化建议、局部改写或向量生成等任务中，优先选择最容易超时的任务进行改造。

---

## v3.5 建议小任务

- v3.5.1 选择首批异步化任务
- v3.5.2 简历解析任务状态化
- v3.5.3 AI 分析任务状态化
- v3.5.4 向量生成任务状态化
- v3.5.5 前端任务状态展示
- v3.5.6 v3.5 联调、审查与日志

进入 v3.5 前再详细展开。

---

# v3.6 - 安全加固与敏感信息治理

状态：未开始

## v3.6 目标

系统性检查和加固认证授权、文件访问、上传限制、敏感配置、日志输出和 AI 数据处理，降低上线风险。

---

## v3.6 建议小任务

- v3.6.1 安全清单整理
- v3.6.2 用户数据访问控制检查
- v3.6.3 文件上传与文件访问安全加固
- v3.6.4 敏感配置治理
- v3.6.5 日志脱敏与错误信息规范
- v3.6.6 AI 输入输出隐私边界说明
- v3.6.7 v3.6 联调、审查与日志

进入 v3.6 前再详细展开。

---

# v3.7 - 可部署配置与环境隔离

状态：未开始

## v3.7 目标

完善本地、开发、生产环境配置，确保数据库、Redis、文件存储、AI 服务、Embedding 服务、日志级别和应用 Profile 可以按环境切换。

---

## v3.7 建议小任务

- v3.7.1 Profile 设计
- v3.7.2 环境变量清单整理
- v3.7.3 application-local / application-prod 配置整理
- v3.7.4 Docker Compose 部署配置整理
- v3.7.5 Nginx 反向代理配置草案
- v3.7.6 部署检查清单
- v3.7.7 v3.7 联调、审查与日志

进入 v3.7 前再详细展开。

---

# v3.8 - 运维文档、架构复盘与阶段收口

状态：未开始

## v3.8 目标

完成 Phase 4 的收口，整理架构演进成果、部署和运维文档、风险记录和后续 Phase 5 产品化优化准备。

---

## v3.8 建议小任务

- v3.8.1 架构演进总结
- v3.8.2 部署文档初版
- v3.8.3 运维文档初版
- v3.8.4 常见问题排查
- v3.8.5 架构图和模块图整理
- v3.8.6 Phase 5 进入条件确认
- v3.8.7 v3.8 阶段日志

进入 v3.8 前再详细展开。

---

## 4. Phase 4 完成后的下一步

Phase 4 完成后，可以进入：

`Phase 5 - 产品化优化、部署上线与项目包装阶段`

Phase 5 再开始重点处理：

- 产品体验复盘。
- 前端 UI/UX 深度优化。
- 核心页面视觉重构。
- 服务器正式部署。
- 域名和 HTTPS。
- 数据备份和运维。
- README、演示文档和项目截图。
- 简历包装与面试讲解材料。
