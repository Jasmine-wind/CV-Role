# Frontend API base URL.
# API request paths already include /api.
# Local dev can use http://localhost:8080.
# Production behind reverse proxy should usually keep this empty.
VITE_API_BASE_URL=http://localhost:8080
