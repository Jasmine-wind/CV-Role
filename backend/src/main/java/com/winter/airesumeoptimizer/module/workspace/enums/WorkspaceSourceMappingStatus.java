package com.winter.airesumeoptimizer.module.workspace.enums;

/** Deterministic SOURCE occurrence to TARGET structure relationship. */
public enum WorkspaceSourceMappingStatus {
    EXACT,
    MERGED,
    SPLIT,
    UNMAPPED,
    AMBIGUOUS
}
